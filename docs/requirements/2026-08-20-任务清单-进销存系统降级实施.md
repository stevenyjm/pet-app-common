# 进销存系统降级实施任务清单

> **文档类型**：任务清单 / 实施计划
> **文档版本**：v1.1
> **编制日期**：2026-08-20
> **关联需求**：[2026-08-20-需求-进销存系统降级与备案.md](./2026-08-20-需求-进销存系统降级与备案.md) v1.2
> **涉及项目**：`pet-app-backend`（Phase 1）、`pet-app-admin-web`（Phase 2）、三端（Phase 3-4）
> **不涉及项目**：`pet-app`（C 端无改动）
> **执行约束**：严格遵循 `multi-project.md` §2 跨项目变更顺序契约；数据库不变更（无 DDL、无迁移脚本）

---

## 状态图例

| 标记 | 含义 |
|------|------|
| ⬜ | 待执行 |
| 🔄 | 进行中 |
| ✅ | 已完成 |
| ❌ | 阻断/有问题 |
| ⏭️ | 跳过（不适用） |

---

## Phase 1：后端实现（pet-app-backend）

> **目标**：改造入库/出库 Service、删除冗余 Route/Service/Cron、精简权限码、新增归档脚本、后端自测
> **涉及项目**：pet-app-backend
> **前置条件**：需求文档 v1.2 已确认全部 12 项决策
> **任务数**：17 项（P1-01~P1-17）
> **状态**：✅ 已完成（2026-08-20）

### 1.1 Service 改造（P1-01~P1-05）

#### P1-01：inventoryService.js 新增 inboundWithBatch 方法 ✅

- **对应决策**：D-1（完全脱离采购单/供应商）、D-11（生产日期/过期日期选填）、D-12（unit_cost 选填）
- **涉及文件**：[services/inventoryService.js](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/services/inventoryService.js)
- **任务描述**：
  - 新增 `inboundWithBatch({ sku_id, quantity, unit_cost?, production_date?, expiry_date?, related_doc_no?, reason? })` 方法
  - 自动生成 `batch_no`（格式：`INB_{YYYYMMDDHHmmss}_{random4}`）
  - 创建 `BatchInfo` 记录（`warehouse_id=1, location_id=1, status='active'`，`purchase_id=null, supplier_id=null`）
  - 若 `expiry_date` 未填且 SKU 有 `shelf_life_days` 且已填 `production_date`，自动推算 `expiry_date = production_date + shelf_life_days`
  - 更新 `InventoryRealTime.available_qty += quantity`（或自动创建记录）
  - 写入 `InventoryFlow(direction='IN', batch_id=新批次, unit_cost, related_doc_no, reason)`
  - 返回 `{ flow_id, batch_id, batch_no }`
- **验收标准**：
  - `node --check services/inventoryService.js` 通过
  - 调用后 `BatchInfo` 表新增一条记录，`batch_no` 唯一
  - `InventoryRealTime.available_qty` 正确增加
  - `InventoryFlow` 表新增一条 `direction='IN'` 记录，`batch_id` 非空

#### P1-02：inventoryService.js 新增 outboundWithFIFO 方法 ✅

- **对应决策**：D-2（新增 deductStockDirectWithFIFO）
- **涉及文件**：[services/inventoryService.js](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/services/inventoryService.js)
- **任务描述**：
  - 新增 `outboundWithFIFO({ sku_id, quantity, related_doc_no?, reason? })` 方法
  - 调用 `deductStockDirectWithFIFO(sku_id, quantity, DEFAULT_WAREHOUSE_ID, DEFAULT_LOCATION_ID)`（P1-03）
  - 写入 `InventoryFlow(direction='OUT', batch_id=分配批次, related_doc_no, reason)`
  - 返回 `{ flow_id, allocations: [{ batch_id, batch_no, allocated_qty }] }`
- **验收标准**：
  - `node --check` 通过
  - 调用后 `InventoryRealTime.available_qty` 正确减少
  - `BatchInfo.quantity` 按 FIFO 顺序扣减
  - `InventoryFlow` 表新增一条 `direction='OUT'` 记录

#### P1-03：inventoryService.js 新增 deductStockDirectWithFIFO 方法 ✅

- **对应决策**：D-2（新增 deductStockDirectWithFIFO）
- **涉及文件**：[services/inventoryService.js](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/services/inventoryService.js)
- **任务描述**：
  - 新增 `deductStockDirectWithFIFO(sku_id, quantity, warehouse_id, location_id)` 方法
  - 与 `deductStockWithFIFO` 区别：**直接扣 `available_qty`，不校验 `locked_qty`，不要求先预占**
  - 校验 `available_qty >= quantity`，不足抛 `INVENTORY_INSUFFICIENT`（400）
  - 查询 `BatchInfo WHERE sku_id, warehouse_id=1, location_id=1, status='active', quantity>0 ORDER BY production_date ASC`
  - 若无批次抛 `NO_AVAILABLE_BATCH`（400）
  - FIFO 分配批次：`batch.quantity -= allocateQty`
  - 更新 `InventoryRealTime.available_qty -= quantity`（乐观锁）
  - 返回分配明细 `[{ batch_id, batch_no, allocated_qty }]`
- **验收标准**：
  - `node --check` 通过
  - 手动出库（未预占）能正常扣减，不抛 `INVENTORY_LOCK_INSUFFICIENT`
  - 批次不足时抛 `NO_AVAILABLE_BATCH`
  - 库存不足时抛 `INVENTORY_INSUFFICIENT`

#### P1-04：outboundService.js 移除 financeService 应收账款调用 ✅

- **对应决策**：D-5（财务不再写入）
- **涉及文件**：[services/outboundService.js](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/services/outboundService.js)
- **任务描述**：
  - 移除 L87-L94 `financeService.createReceivable` 调用逻辑
  - 移除 `const financeService = require('./financeService')` 引用（若该文件仅此处使用）
  - 保留销售出库主链路（`deductStockWithFIFO`、`SalesOutbound` 创建等）不变
- **验收标准**：
  - `node --check` 通过
  - 订单支付触发出库不再创建 `AccountsReceivable` 记录
  - 销售出库主链路功能不受影响

#### P1-05：batchService.js 精简为仅查询方法 ✅

- **对应决策**：D-7（批次数据查看入口）
- **涉及文件**：[services/batchService.js](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/services/batchService.js)
- **任务描述**：
  - 保留方法：`listBatches`、`getBatchById`、`getBatchByNo`（仅查询）
  - 删除方法：`createBatch`、`updateBatch`、`deleteBatch`、`allocateBatchFIFO`、`deductBatchStock`、`addBatchStock`、`checkExpiryStatus`
  - 注意：`allocateBatchFIFO` / `deductBatchStock` 若被 `inventoryService.deductStockWithFIFO` 引用，需确认引用关系（可能内联在 inventoryService 中，不依赖 batchService）
- **验收标准**：
  - `node --check` 通过
  - 无其他 Service 引用已删除方法（需 Grep 确认）
  - 批次查询 API 正常工作

### 1.2 Route 改造与删除（P1-06~P1-08）

#### P1-06：batchRoutes.js 精简为仅查询端点 ✅

- **对应决策**：D-7（批次数据查看入口）
- **涉及文件**：[routes/admin/batchRoutes.js](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/routes/admin/batchRoutes.js)
- **任务描述**：
  - 保留端点：`GET /`（批次列表查询，支持 `skuId` 过滤）、`GET /:id`（批次详情）
  - 删除端点：`POST /`、`PUT /:id`、`DELETE /:id`、`POST /:id/stock/add`、`POST /:id/stock/deduct` 等增删改端点
  - 保留 `batch:view` 权限校验
- **验收标准**：
  - `node --check` 通过
  - `GET /admin/batches?skuId=...` 返回该 SKU 下活跃批次列表
  - 增删改端点返回 404

#### P1-07：inventoryRoutes.js 改造 inbound/outbound 端点 ✅

- **对应决策**：D-1、D-2
- **涉及文件**：[routes/admin/inventoryRoutes.js](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/routes/admin/inventoryRoutes.js)
- **任务描述**：
  - `POST /inbound` 改为调用 `inventoryService.inboundWithBatch`（P1-01），请求体精简为 `{ sku_id, quantity, unit_cost?, production_date?, expiry_date?, related_doc_no?, reason? }`
  - `POST /outbound` 改为调用 `inventoryService.outboundWithFIFO`（P1-02），请求体精简为 `{ sku_id, quantity, related_doc_no?, reason? }`
  - 保留端点：`GET /`（库存列表）、`GET /flow`（流水查询）、`GET /flow/summary`（流水汇总）、`GET /dashboard`（概览）、`POST /adjust`（库存调整，D-4）
  - 移除请求体中的 `warehouse_id` / `location_id` 字段（后端固定使用默认值）
- **验收标准**：
  - `node --check` 通过
  - `POST /admin/inventory/inbound` 返回 `{ flow_id, batch_id, batch_no }`
  - `POST /admin/inventory/outbound` 返回 `{ flow_id, allocations }`
  - `GET /admin/inventory` 不再需要 `warehouseId` / `locationId` 参数

#### P1-08：删除后端 Route 文件 ✅

- **涉及文件**：
  - `routes/admin/warehouseRoutes.js`
  - `routes/admin/locationRoutes.js`
  - `routes/admin/expiryWarningRoutes.js`
  - `routes/admin/traceRoutes.js`
  - `routes/admin/transferRoutes.js`
  - `routes/admin/inventoryCheckRoutes.js`
  - `routes/admin/purchaseRoutes.js`
  - `routes/admin/supplierRoutes.js`
  - `routes/admin/accountsPayableRoutes.js`
  - `routes/admin/financeRoutes.js`
  - `routes/admin/notificationRoutes.js`
- **任务描述**：删除上述 11 个 Route 文件
- **验收标准**：
  - 文件已删除
  - `routes/admin/index.js` 中对应的 `router.use` 行已移除（P1-11）
  - 无其他文件引用已删除的 Route

### 1.3 Service 与 Cron 删除（P1-09~P1-10）

#### P1-09：删除后端 Service 文件 ✅

- **对应决策**：D-1（采购/供应商）、D-3（临期/通知）、D-5（财务）
- **涉及文件**：
  - `services/transferService.js`
  - `services/inventoryCheckService.js`
  - `services/traceService.js`
  - `services/expiryWarningService.js`
  - `services/financeService.js`
  - `services/supplierService.js`
  - `services/notificationService.js`
  - `services/purchaseService.js`
- **任务描述**：删除上述 8 个 Service 文件
- **验收标准**：
  - 文件已删除
  - 无其他文件 `require` 已删除的 Service（需 Grep 确认；`outboundService.js` 中的 `financeService` 引用已在 P1-04 移除）
  - `node --check` 所有引用过这些 Service 的文件通过

#### P1-10：删除 Cron Service 文件 ✅

- **对应决策**：D-3（禁用定时任务）
- **涉及文件**：
  - `services/expiryWarningCronService.js`
  - `services/lowStockCronService.js`
  - `services/notificationCleanupCronService.js`
- **任务描述**：删除上述 3 个 Cron Service 文件
- **保留**：`services/orderExpiryCronService.js`（D-8 已确认保留）
- **验收标准**：
  - 文件已删除
  - `server.js` 或 cron 注册文件中对应的注册已移除（P1-12）
  - `orderExpiryCronService` 注册保留

### 1.4 注册与权限精简（P1-11~P1-13）

#### P1-11：routes/admin/index.js 更新路由注册 ✅

- **涉及文件**：[routes/admin/index.js](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/routes/admin/index.js)
- **任务描述**：
  - 移除已删除模块的 `router.use` 行：`warehouseRoutes`、`locationRoutes`、`expiryWarningRoutes`、`traceRoutes`、`transferRoutes`、`inventoryCheckRoutes`、`purchaseRoutes`、`supplierRoutes`、`accountsPayableRoutes`、`financeRoutes`、`notificationRoutes`
  - **保留**：`batchRoutes`（D-7 批次查询）、`orderExpiryRoutes`（D-8 订单过期）、`inventoryRoutes`（库存/入库/出库）
  - 移除对应的 `require` 语句
- **验收标准**：
  - `node --check` 通过
  - 已删除模块的 API 返回 404
  - 保留模块的 API 正常工作

#### P1-12：server.js / cron 注册文件更新 ✅

- **涉及文件**：[server.js](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/server.js) 或 cron 注册文件
- **任务描述**：
  - 移除 `expiryWarningCronService`、`lowStockCronService`、`notificationCleanupCronService` 的注册与启动
  - **保留** `orderExpiryCronService` 的注册与启动
  - 移除对应的 `require` 语句
- **验收标准**：
  - `node --check` 通过
  - 服务启动后无 Cron 找不到的报错
  - `orderExpiryCronService` 正常运行

#### P1-13：utils/inventoryPermissions.js 精简权限码 ✅

- **对应决策**：D-7（保留 batch:view）、D-10（清理已删除模块枚举）
- **涉及文件**：[utils/inventoryPermissions.js](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/utils/inventoryPermissions.js)
- **任务描述**：
  - 保留权限码：`inventory:view`、`inventory:inbound`、`inventory:outbound`、`audit_log:view`、`batch:view`（D-7 批次查询）
  - 删除权限码：`inventory:dashboard`、`warehouse:view`、`location:view`、`expiry:view`、`expiry:config`、`trace:view`、`transfer:view`、`check:view`、`purchase:view`、`supplier:view`、`finance:view`、`notification:view`
- **验收标准**：
  - `node --check` 通过
  - 权限码定义文件精简至上述 5 项
  - 数据库中角色权限关联表的历史权限码记录保留（不删除）

### 1.5 验证与自测（P1-14~P1-17）

#### P1-14：语法检查所有变更文件 ✅

- **任务描述**：对所有变更/新增的后端 `.js` 文件执行 `node --check`
- **涉及文件**：P1-01~P1-13 涉及的所有文件
- **验收标准**：所有文件 `node --check` 通过，无语法错误

#### P1-15：数据库校验 ✅

- **任务描述**：执行 `npm run verify:db`，确认数据库表结构无变化
- **验收标准**：`verify:db` 校验通过，无字段/表缺失

#### P1-16：新增历史批次归档脚本 ✅

- **对应决策**：D-9（历史批次数据归档）
- **涉及文件**：新增 `scripts/archive-historical-batches.js`
- **任务描述**：
  - 编写一次性归档脚本
  - 将 `batch_info` 表中 `quantity=0` 且 `status='active'` 的历史批次 `status` 置为 `empty`
  - 输出归档统计（影响行数）
  - 支持 `--dry-run` 参数预览
- **验收标准**：
  - `node --check scripts/archive-historical-batches.js` 通过
  - `--dry-run` 模式输出正确统计
  - 执行后 `quantity=0` 的批次 `status` 变为 `empty`
  - `quantity>0` 的批次不受影响

#### P1-17：后端自测 ✅

- **任务描述**：启动后端服务，通过 curl/Postman 验证关键 API
- **测试用例**：
  1. `POST /admin/inventory/inbound`（入库）→ 验证 `batch_no` 自动生成、`BatchInfo` 创建、`InventoryRealTime.available_qty` 增加
  2. `POST /admin/inventory/outbound`（手动出库）→ 验证 `available_qty` 减少、`BatchInfo.quantity` FIFO 扣减
  3. `GET /admin/inventory`（库存查询）→ 验证返回结果
  4. `GET /admin/batches?skuId=...`（批次查询）→ 验证返回该 SKU 下活跃批次
  5. `POST /admin/inventory/adjust`（库存调整）→ 验证 `available_qty` 调整
  6. `GET /admin/inventory/dashboard`（概览）→ 验证概览数据
  7. 订单支付触发出库 → 验证销售出库链路正常（`deductStockWithFIFO` 能找到批次）
  8. 已删除模块 API（如 `GET /admin/warehouses`）→ 验证返回 404
- **验收标准**：所有测试用例通过

---

## Phase 2：管理端实现（pet-app-admin-web）

> **目标**：删除冗余页面、改造三个保留页面、精简 API/Type/权限码、构建验证
> **涉及项目**：pet-app-admin-web
> **前置条件**：Phase 1 完成，后端 API 已就位
> **任务数**：12 项（P2-01~P2-12）

### 2.1 路由与页面删除（P2-01~P2-02）

#### P2-01：router/index.ts 删除进销存分组路由项 ⬜

- **涉及文件**：[src/router/index.ts](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-admin-web/src/router/index.ts)
- **任务描述**：
  - 删除进销存分组中 #1（库存概览）、#5-#16 路由项
  - **保留**：#2 库存管理（`/inventory/list`）、#3 入库管理（`/inventory/inbound`）、#4 出库管理（`/inventory/outbound`）
  - 移除已删除路由项的 `component` 动态导入
- **验收标准**：
  - 路由配置中仅保留 #2、#3、#4 三项
  - 已删除路由路径访问时跳转 404

#### P2-02：删除 13 个 vue 页面文件 ⬜

- **涉及文件**：`src/views/inventory/` 下以下文件：
  - `InventoryDashboard.vue`
  - `Warehouse.vue`
  - `Location.vue`
  - `Batch.vue`
  - `ExpiryWarning.vue`
  - `ExpiryWarningConfig.vue`
  - `TraceQuery.vue`
  - `Transfer.vue`
  - `InventoryCheck.vue`
  - `Purchase.vue`
  - `Supplier.vue`
  - `FinanceReport.vue`
  - `NotificationCenter.vue`
- **任务描述**：删除上述 13 个文件
- **验收标准**：
  - 文件已删除
  - 无其他文件引用已删除的页面组件（需 Grep 确认）

### 2.2 保留页面改造（P2-03~P2-05）

#### P2-03：List.vue 改造 ⬜

- **对应决策**：D-4（调整库存）、D-6（概览卡片）、D-7（查看批次）
- **涉及文件**：[src/views/inventory/List.vue](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-admin-web/src/views/inventory/List.vue)
- **任务描述**：
  - 移除仓库下拉（`warehouseId` 固定为 1，不再调用 `getWarehouses`）
  - 移除库位下拉（`locationId` 固定为 1，不再调用 `getLocations`）
  - 移除仓库名/库位编码列
  - 顶部增加简化概览卡片：调用 `getInventoryDashboard()`，展示总库存量、SKU 数、低库存数、今日出入库量（D-6）
  - 每行增加"查看批次"按钮：点击弹窗，调用 `getBatches({ skuId })` 显示该 SKU 下活跃批次列表（batch_no、生产日期、过期日期、数量、状态）（D-7）
  - 每行增加"调整库存"按钮：点击弹窗，输入新数量 + 原因，调用 `adjustInventory({ skuId, newQuantity, reason })`（D-4）
  - 保留：导出 Excel、SKU 搜索、库存状态徽标
- **验收标准**：
  - 页面加载不再请求仓库/库位下拉数据
  - 概览卡片正确展示
  - "查看批次"弹窗正确显示批次列表
  - "调整库存"弹窗提交后 `available_qty` 更新

#### P2-04：Inbound.vue 改造 ⬜

- **对应决策**：D-1（脱离采购单）、D-11（生产日期/过期日期选填）、D-12（unit_cost 选填）
- **涉及文件**：[src/views/inventory/Inbound.vue](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-admin-web/src/views/inventory/Inbound.vue)
- **任务描述**：
  - 移除仓库下拉（固定 `warehouseId=1`）
  - 移除库位下拉（固定 `locationId=1`）
  - 移除采购单下拉（D-1，不再依赖采购单）
  - 移除批次号输入框（后端 `inboundWithBatch` 自动生成）
  - 保留 SKU、数量（必填）
  - 生产日期（选填，D-11）
  - 过期日期（选填，D-11；提示"不填且 SKU 有保质期将自动推算"）
  - 单价 unit_cost（选填，D-12）
  - 关联单号（选填，用于追溯）
  - 入库记录列表：沿用 `getInventoryFlow({ direction: 'IN' })`，增加 `batch_no` 列
  - 入库表单提交调用 `createInbound({ skuId, quantity, unitCost?, productionDate?, expiryDate?, relatedDocNo?, reason? })`
- **验收标准**：
  - 表单不再有仓库/库位/采购单/批次号字段
  - 提交后返回 `batch_no` 并显示在列表中
  - 生产日期/过期日期/单价非必填

#### P2-05：Outbound.vue 改造 ⬜

- **对应决策**：D-2（手动出库走 deductStockDirectWithFIFO）
- **涉及文件**：[src/views/inventory/Outbound.vue](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-admin-web/src/views/inventory/Outbound.vue)
- **任务描述**：
  - 手动出库表单：移除仓库/库位下拉（固定 `warehouseId=1, locationId=1`）
  - 手动出库表单：保留 SKU、数量（必填）、原因/关联单号（选填）
  - 手动出库提交调用 `createOutbound({ skuId, quantity, relatedDocNo?, reason? })`（后端走 `outboundWithFIFO`）
  - 销售出库单列表：沿用 `getOutboundList`，不变
  - 确认/取消销售出库单：沿用 `confirmOutbound` / `cancelOutbound`，不变
  - 手动出库记录列表：增加 `getInventoryFlow({ direction: 'OUT' })` 查询（可选）
- **验收标准**：
  - 手动出库表单不再有仓库/库位字段
  - 提交后 `available_qty` 减少、批次扣减
  - 销售出库单列表与确认/取消功能正常

### 2.3 API/Type/权限精简（P2-06~P2-11）

#### P2-06：api/inventory.ts 精简 ⬜

- **涉及文件**：[src/api/inventory.ts](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-admin-web/src/api/inventory.ts)
- **任务描述**：
  - 移除 API：仓库增删改（`createWarehouse` / `updateWarehouse` / `deleteWarehouse`）、库位增删改、采购、供应商、调拨、盘点、临期预警、追溯、通知等
  - **保留** API：`getInventoryList`、`getInventoryFlow`、`getInventoryFlowSummary`、`getInventoryDashboard`、`createInbound`（改造）、`createOutbound`（改造）、`getOutboundList`、`confirmOutbound`、`cancelOutbound`、`adjustInventory`、`getBatches`（D-7）、`getBatch`
  - `createInbound` / `createOutbound` 请求参数精简（移除 `warehouseId` / `locationId`）
- **验收标准**：
  - 已删除 API 函数不再导出
  - 保留 API 函数参数类型正确

#### P2-07：删除 api/purchase.ts、api/supplier.ts、api/storage.ts ⬜

- **对应决策**：D-1（脱离采购/供应商）
- **涉及文件**：
  - `src/api/purchase.ts`
  - `src/api/supplier.ts`
  - `src/api/storage.ts`（若仅进销存使用）
- **任务描述**：删除上述文件（若 `storage.ts` 被其他模块引用则保留，需 Grep 确认）
- **验收标准**：文件已删除，无其他文件引用

#### P2-08：types/inventory.ts 精简 ⬜

- **涉及文件**：[src/types/inventory.ts](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-admin-web/src/types/inventory.ts)
- **任务描述**：
  - 移除已删除模块的类型定义（仓库增删改、库位增删改、采购、供应商、调拨、盘点、临期预警、追溯、通知等）
  - 保留类型：库存列表、库存流水、概览、入库/出库 payload、销售出库单、批次查询、库存调整
  - `InboundPayload` 精简为 `{ skuId, quantity, unitCost?, productionDate?, expiryDate?, relatedDocNo?, reason? }`
  - `OutboundPayload` 精简为 `{ skuId, quantity, relatedDocNo?, reason? }`
- **验收标准**：类型定义精简，与 API 参数一致

#### P2-09：删除 types/purchase.ts、types/supplier.ts、types/storage.ts ⬜

- **涉及文件**：
  - `src/types/purchase.ts`
  - `src/types/supplier.ts`
  - `src/types/storage.ts`（若仅进销存使用）
- **任务描述**：删除上述文件
- **验收标准**：文件已删除，无其他文件引用

#### P2-10：types/inventory.ts 中 AUDIT_LOG_MODULES 清理 ⬜

- **对应决策**：D-10（清理已删除模块枚举）
- **涉及文件**：[src/types/inventory.ts](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-admin-web/src/types/inventory.ts)
- **任务描述**：
  - `AUDIT_LOG_MODULES` 删除枚举：`batch`（管理操作已删除，仅查询不记日志）、`expiry`、`transfer`、`purchase`、`supplier`、`finance`
  - `AUDIT_LOG_MODULE_LABELS` 同步清理对应标签
  - 保留：`inventory`、`order`、`payment`、`pet`、`admin`、`system` 等非进销存删除模块枚举
- **验收标准**：枚举清理完成，操作日志页面模块筛选下拉不再显示已删除模块

#### P2-11：INVENTORY_PERMISSIONS 精简 ⬜

- **对应决策**：D-7（保留 batch:view）、D-10
- **涉及文件**：权限码定义文件（`src/constants/permissions.ts` 或类似）
- **任务描述**：
  - 保留权限码：`inventory:view`、`inventory:inbound`、`inventory:outbound`、`audit_log:view`、`batch:view`（D-7）
  - 删除权限码：`inventory:dashboard`、`warehouse:view`、`location:view`、`expiry:view`、`expiry:config`、`trace:view`、`transfer:view`、`check:view`、`purchase:view`、`supplier:view`、`finance:view`、`notification:view`
- **验收标准**：权限码定义精简至上述 5 项

### 2.4 构建验证（P2-12）

#### P2-12：构建验证 ⬜

- **任务描述**：执行 `npm run build`，确认无错误无警告
- **验收标准**：
  - 构建成功，0 错误 0 警告
  - 无未解析的导入（已删除文件无残留引用）
  - 无未使用的导出（dead code 已清理）

---

## Phase 3：三端对齐检查

> **目标**：按 `multi-project.md` §3 三端对齐检查清单逐项检查
> **涉及项目**：三端
> **前置条件**：Phase 1、Phase 2 完成
> **任务数**：4 项（P3-01~P3-04）

#### P3-01：API 规范对齐 ⬜

- **检查项**：
  - [ ] 降级后 API 规范文档已更新（Phase 4 P4-03 创建）
  - [ ] 后端路由/控制器实现与规范一致
  - [ ] 管理端 API 调用与规范一致
  - [ ] C 端 API 调用不受影响（本次无 C 端改动）
  - [ ] 错误码使用一致（`NO_AVAILABLE_BATCH`、`INVENTORY_INSUFFICIENT` 等）
- **验收标准**：所有检查项通过

#### P3-02：字段命名对齐 ⬜

- **检查项**：
  - [ ] 数据库字段使用 snake_case（本次无 DDL 变更）
  - [ ] API 路径参数使用 snake_case
  - [ ] 管理端 API 响应字段：camelCase（与现有管理端规范一致）
  - [ ] 前端消费时字段名与后端响应一致
- **验收标准**：所有检查项通过

#### P3-03：时间字段对齐 ⬜

- **检查项**：
  - [ ] 所有时间字段标注时区（北京时间 +08:00）
  - [ ] 后端返回 ISO 8601 格式时间字符串
  - [ ] 前端统一时间格式化函数
- **验收标准**：所有检查项通过

#### P3-04：新增字段检查 ⬜

- **检查项**：
  - [ ] 本次降级**无新增字段**（仅删除/精简）
  - [ ] 数据库表字段无变化（`verify:db` 已在 P1-15 验证）
  - [ ] Sequelize Model 无新增字段
  - [ ] `scripts/schema.sql` 无变化
  - [ ] `scripts/verify-db.js` 校验项无变化
- **验收标准**：所有检查项通过

---

## Phase 4：联调与文档归档

> **目标**：联调验证、文档归档、四端日志同步
> **涉及项目**：三端
> **前置条件**：Phase 1、2、3 完成
> **任务数**：6 项（P4-01~P4-06）

#### P4-01：联调验证 ⬜

- **任务描述**：全链路联调测试
- **测试用例**：
  1. 管理端入库 → 验证 `batch_no` 生成、库存增加、批次创建
  2. C 端下单 → 验证库存预占（`available_qty` 减少、`locked_qty` 增加）
  3. C 端支付 → 验证销售出库（`deductStockWithFIFO` 找到批次并扣减、`locked_qty` 减少）
  4. 管理端手动出库 → 验证 `available_qty` 减少、批次 FIFO 扣减
  5. 管理端库存调整 → 验证 `available_qty` 调整
  6. 管理端查看批次 → 验证弹窗显示批次列表
  7. 订单过期 → 验证 `orderExpiryCronService` 释放预占
  8. 已删除模块页面访问 → 验证 404 跳转
- **验收标准**：所有测试用例通过

#### P4-02：更新需求文档 v1.2 ⬜

- **涉及文件**：[2026-08-20-需求-进销存系统降级与备案.md](./2026-08-20-需求-进销存系统降级与备案.md)
- **任务描述**：
  - 在需求文档中标注各 Phase 完成状态
  - 更新 §9 阶段规划状态为"已完成"
  - 更新 §12 实施任务清单索引，标注任务清单完成状态
  - 版本号升级至 v1.3（最终归档版）
- **验收标准**：需求文档状态更新完成

#### P4-03：新建降级后 API 规范文档 ⬜

- **涉及文件**：新建 `pet-app-common/docs/api-specs/2026-08-20-API规范-进销存降级后入库出库.md`
- **任务描述**：
  - 文档化降级后的入库/出库 API 规范
  - 包含：路径、请求参数、响应字段、错误码、示例
  - 废弃 [进销存-通知消息与补充功能-API规范.md](../api-specs/进销存-通知消息与补充功能-API规范.md) 中已删除模块的接口
- **验收标准**：API 规范文档创建完成，与后端实现一致

#### P4-04：更新后端开发说明 ⬜

- **涉及文件**：[pet-app-backend/docs/项目开发说明-后端.md](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-backend/docs/项目开发说明-后端.md)
- **任务描述**：
  - 更新进销存章节：删除已删除模块的描述
  - 新增降级后入库/出库 Service 方法说明（`inboundWithBatch` / `outboundWithFIFO` / `deductStockDirectWithFIFO`）
  - 更新权限码清单
  - 更新 Cron 服务清单
  - 版本号递增
- **验收标准**：后端开发说明更新完成

#### P4-05：更新管理端开发说明 ⬜

- **涉及文件**：[pet-app-admin-web/docs/项目开发说明-Web前端.md](file:///c:/Users/22188/Desktop/宠物生鲜电商开发项目/pet-app-admin-web/docs/项目开发说明-Web前端.md)
- **任务描述**：
  - 更新进销存章节：删除已删除页面的描述
  - 更新三个保留页面的改造说明
  - 更新 API/Type/权限码清单
  - 版本号递增
- **验收标准**：管理端开发说明更新完成

#### P4-06：四端 docs/yjm_daily 同步 ⬜

- **涉及文件**：
  - `pet-app-backend/docs/yjm_daily/2026-08-20.md`
  - `pet-app-admin-web/docs/yjm_daily/2026-08-20.md`
  - `pet-app-common/docs/yjm_daily/2026-08-20.md`
  - `pet-app/docs/yjm_daily/2026-08-20.md`（若 C 端有日志记录需求；本次无 C 端改动，可仅记录"进销存降级不影响 C 端"）
- **任务描述**：
  - 在各项目今日记录文档中追加进销存降级实施摘要
  - 记录 Phase 1~4 完成情况
  - 记录关键决策点
  - 记录变更文件清单
- **验收标准**：四端日志同步完成

---

## 任务统计

| 阶段 | 任务数 | 涉及项目 | 状态 |
|------|--------|----------|------|
| Phase 1 | 17 项（P1-01~P1-17） | pet-app-backend | ✅ 已完成 |
| Phase 2 | 12 项（P2-01~P2-12） | pet-app-admin-web | ⬜ 待启动 |
| Phase 3 | 4 项（P3-01~P3-04） | 三端 | ⬜ 待启动 |
| Phase 4 | 6 项（P4-01~P4-06） | 三端 | ⬜ 待启动 |
| **合计** | **39 项** | - | 🔄 进行中 |

---

## 修订记录

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| v1.0 | 2026-08-20 | - | 初版：基于需求文档 v1.2 全部 12 项决策确认结果，拆解为 4 阶段 39 项任务 |
| v1.1 | 2026-08-20 | - | Phase 1 全部 17 项任务（P1-01~P1-17）完成标注：Service 改造、Route 删除、Cron 清理、权限精简、归档脚本、后端自测均通过 |
