# 备案 · 第3批仅缺 is_deleted 类表基础字段与逻辑删除规范合规修复

> 关联文档：
> - [2026-08-19-分析报告-数据库基础字段与逻辑删除规范合规审计.md](../2026-08-19-分析报告-数据库基础字段与逻辑删除规范合规审计.md) §三·第三档
>
> 生效日期：2026-08-19
> 变更类型：数据库 Schema 变更（新增字段），12 张表向后兼容，3 张表部分不兼容

## 1. 变更范围

第三档「仅缺 is_deleted（或 created_at/updated_at 命名稍不规范）+ 轻微字段缺失」共 **15 张表**：
- 12 张表已合规（有 created_at/updated_at），仅缺少 is_deleted
- 3 张表使用 create_time（无 update_time），需要补齐 created_at/updated_at/is_deleted

## 2. 字段统一定义

| 字段 | DDL 定义 |
|------|----------|
| `created_at` | `DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）'` |
| `updated_at` | `DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）'` |
| `is_deleted` | `TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除'` |

## 3. 15 张表变更计划

### 3.1 3 张 created_at/updated_at 命名不规范表（需要 RENAME + 补字段）

| # | 表名 | 变更 |
|---|------|------|
| 1 | `user_email_verify` | `create_time` → `created_at`（RENAME COLUMN）；补 `updated_at` + `is_deleted` |
| 2 | `pet_meal_template` | `create_time` → `created_at`（RENAME COLUMN）；补 `updated_at` + `is_deleted` |
| 3 | `pet_health_record` | `create_time` → `created_at`（RENAME COLUMN）；补 `updated_at` + `is_deleted` |

### 3.2 12 张仅缺 is_deleted 的合规表（直接 ADD）

| # | 表名 | 变更 |
|---|------|------|
| 4 | `user_notification_setting` | 补 `is_deleted` |
| 5 | `user_point_balance` | 补 `is_deleted` |
| 6 | `user_subscription` | 补 `is_deleted` |
| 7 | `pet_breed_dict` | 补 `is_deleted` |
| 8 | `pet_breed_tag_dict` | 补 `is_deleted` |
| 9 | `pet_nutritional_requirement` | 补 `is_deleted` |
| 10 | `pet_meal_plan` | 补 `is_deleted` |
| 11 | `pet_meal_template_meal` | 补 `is_deleted` |
| 12 | `pet_meal_formula_item` | 补 `is_deleted` |
| 13 | `pet_exercise_plan` | 补 `is_deleted` |
| 14 | `reminder_config` | 补 `is_deleted` |
| 15 | `admin_message_template` | 补 `is_deleted` |

## 4. 向后兼容性评估

| 变更 | 向后兼容 | 影响 |
|------|---------|------|
| 12 张表新增 `is_deleted` 默认 0 | ✅ 兼容 | 无影响 |
| 3 张表 `create_time` → `created_at` | ❌ 不兼容 | 后端代码字段引用需同步改名（如 WHERE create_time 条件）|
| 3 张表补 `updated_at` 默认值 | ✅ 兼容 | 无影响 |
| 3 张表补 `is_deleted` 默认 0 | ✅ 兼容 | 无影响 |

## 5. 实施顺序

1. ✅ 本文档创建
2. 🔲 schema.sql 同步 15 张表建表语句
3. 🔲 15 个 Sequelize Model 同步
4. 🔲 迁移脚本 v46（migrate-schema-v46-base-fields-batch3.js）+ package.json
5. 🔲 verify-db.js 同步 15 张表字段
6. 🔲 三端文档同步 + 日志
7. 🔲 运行时自测
