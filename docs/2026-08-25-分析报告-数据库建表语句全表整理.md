# 数据库建表语句全表整理

> **创建日期**：2026-08-25
> **数据来源**：pet-app-backend/scripts/schema.sql
> **数据库**：MySQL 8.0+ utf8mb4，库名 pet_app
> **文档目的**：汇总当前后端数据库所有表的建表语句，供三端开发对齐参考
> **规范依据**：multi-project.md §7 数据库表项描述规范、§8 数据库基础字段与逻辑删除规范

## 一、文档说明

- 本档以"建表语句汇总"为核心目的，每张表附带 CREATE TABLE 语句。
- 字段详细说明见建表语句中各字段的 COMMENT。
- 部分表标注 @deprecated 或 DEPRECATED，保留用于历史审计，详见建表语句上方注释。
- 豁免 is_deleted 的表（Token/审计流水/会话消息）已在表 COMMENT 中说明豁免理由。
- 表按业务模块分类，模块内按 schema.sql 出现顺序排列。
- 全表共 105 张（含 @deprecated 表与 DEPRECATED 字段表，均按现状保留）。
- 已于 Schema v51 物理删除的 `pet_meal_session_pet`、`pet_exercise_session_pet` 仅有注释行，无建表语句，本档不再收录。
- 建表语句完整复制自 schema.sql，保留所有字段、约束、索引、外键、COMMENT，不改写；语句上方的注释行（以 `--` 或 `/* */` 标注的说明）一并保留在代码块内。

## 二、表清单总览

| 模块 | 表名 | 说明 | 豁免 is_deleted | @deprecated |
|------|------|------|:---:|:---:|
| 用户体系 | user_account | 用户账号表（微信 openid 主表） | 否 | 否 |
| 用户体系 | user_profile | 用户资料表（昵称/头像/邀请码） | 否 | 否 |
| 用户体系 | user_email_verify | 邮箱验证码表 | 否 | 否 |
| 用户体系 | user_email_send_log | 邮件发送流水表 | 是 | 否 |
| 用户体系 | user_login_log | 用户登录流水表 | 是 | 否 |
| 用户体系 | user_address | 用户收货地址表 | 否 | 否 |
| 用户体系 | user_browse_log | 用户浏览流水表 | 是 | 否 |
| 用户体系 | user_refresh_token | 用户 Refresh Token 表 | 是 | 否 |
| 用户体系 | user_message | 用户站内消息表（会话消息类） | 是 | 否 |
| 用户体系 | user_preferences | 用户偏好设置表 | 否 | 否 |
| 用户体系 | user_feedback | 用户反馈表 | 否 | 否 |
| 用户体系 | user_sms_log | 用户短信流水表 | 是 | 否 |
| 用户体系 | user_notification_setting | 用户通知设置表 | 否 | 否 |
| 用户体系 | user_point_balance | 用户积分余额表 | 否 | 否 |
| 用户体系 | user_subscription | 用户订阅表 | 否 | 否 |
| 商品体系 | product_category | 商品分类表 | 否 | 否 |
| 商品体系 | product | 商品表 | 否 | 否 |
| 商品体系 | product_sku | 商品 SKU 表 | 否 | 否 |
| 交易订单 | cart | 购物车表 | 否 | 否 |
| 交易订单 | order_main | 订单主表 | 否 | 否 |
| 交易订单 | order_detail | 订单明细表 | 否 | 否 |
| 交易订单 | payment_log | 支付流水表 | 是 | 否 |
| 交易订单 | logistics | 物流表 | 否 | 否 |
| 交易订单 | after_sales | 售后单表（旧） | 否 | 否 |
| 交易订单 | order_review | 订单评价表 | 否（历史未补） | 否 |
| 交易订单 | order_review_item | 评价项表 | 否 | 否 |
| 交易订单 | order_review_draft | 评价草稿表 | 否 | 否 |
| 交易订单 | refund_record | 退款单表 | 否 | 否 |
| 交易订单 | after_sales_request | 售后请求表 | 否 | 否 |
| 交易订单 | after_sales_item | 售后明细表 | 否 | 否 |
| 资讯内容 | article | 资讯表 | 否 | 否 |
| 资讯内容 | cms_page | CMS 页面表 | 否 | 否 |
| 资讯内容 | article_favorite | 资讯收藏表 | 否（历史未补） | 否 |
| 资讯内容 | article_category | 资讯分类表 | 否 | 否 |
| 宠物档案与记录 | pet | 宠物档案表 | 否 | 否 |
| 宠物档案与记录 | pet_owner | 宠物所属关系表 | 否 | 否 |
| 宠物档案与记录 | pet_license | 宠物证件表 | 否 | 否 |
| 宠物档案与记录 | pet_assessment | 宠物体型评估表 | 否 | 否 |
| 宠物档案与记录 | pet_daily_status | 宠物每日状态表（3 字段 DEPRECATED） | 否 | 否（字段级 DEPRECATED） |
| 宠物档案与记录 | pet_allergy_ingredient | 宠物过敏食材关联表 | 否 | 否 |
| 宠物档案与记录 | pet_health_record | 宠物健康档案表 | 否 | 否 |
| 宠物档案与记录 | pet_breed_dict | 宠物品种字典表 | 否 | 否 |
| 宠物档案与记录 | pet_breed_tag_dict | 宠物品种标签字典表 | 否 | 否 |
| 宠物档案与记录 | pet_nutritional_requirement | 宠物营养需求配置表 | 否 | 否 |
| 宠物档案与记录 | pet_mood_record | 宠物心情记录表 | 否 | 否 |
| 宠物档案与记录 | pet_stool_record | 宠物排便记录表 | 否 | 否 |
| 宠物档案与记录 | pet_daily_health_metric | 宠物每日健康指标表 | 否 | 否 |
| 餐食与运动记录 | pet_meal_session | 餐食会话表 | 否 | 否 |
| 餐食与运动记录 | pet_meal_item | 餐食项表 | 否 | 否 |
| 餐食与运动记录 | pet_meal_template | 餐食模板表 | 否 | 否 |
| 餐食与运动记录 | pet_meal_template_item | 餐食模板项表 | 否 | 否 |
| 餐食与运动记录 | pet_meal_template_meal | 模板-用餐日期关联表 | 否 | 否 |
| 餐食与运动记录 | pet_meal_formula_item | 食谱配方明细表 | 否 | 否 |
| 餐食与运动记录 | pet_meal_plan | 喂食计划表 | 否 | 否 |
| 餐食与运动记录 | pet_exercise_session | 运动会话表 | 否 | 否 |
| 餐食与运动记录 | pet_exercise_template | 运动模板表 | 否 | 否 |
| 餐食与运动记录 | pet_exercise_plan | 运动计划表 | 否 | 否 |
| 营养因子 | nutrition_extra_dict | 额外营养素字典 | 否 | 否 |
| 营养因子 | reminder_tag_dict | 提醒标签字典 | 否 | 是（2026-08-20） |
| 营养因子 | pet_nutrition_factor | 宠物营养因子（矩阵行） | 否 | 否 |
| 营养因子 | pet_nutrition_factor_dimension | 宠物营养因子维度（矩阵列） | 否 | 否 |
| 营养因子 | pet_nutrition_factor_value | 宠物营养因子矩阵单元格值 | 否 | 否 |
| 家庭组 | family_group | 家庭组表 | 否 | 否 |
| 家庭组 | family_group_member | 家庭组成员表 | 否 | 否 |
| 家庭组 | family_group_pet | 家庭组宠物表 | 否 | 否 |
| 家庭组 | family_group_invitation | 家庭组邀请表 | 否 | 否 |
| 家庭组 | family_group_member_removal | 家庭组成员移除表 | 否 | 否 |
| 家庭组 | family_group_pet_removal_request | 家庭组宠物移除申请表 | 否 | 否 |
| 家庭组 | family_group_pet_removal_vote | 家庭组宠物移除投票表 | 否 | 否 |
| 家庭组 | family_group_tag | 家庭组标签关联表 | 否 | 否 |
| 管理后台 | admin_user | 管理员用户表 | 否 | 否 |
| 管理后台 | admin_refresh_token | 管理员 Refresh Token 表 | 是 | 否 |
| 管理后台 | shop_config | 店铺配置表 | 否 | 否 |
| 管理后台 | admin_message_job | 管理员消息任务表 | 否 | 否 |
| 管理后台 | admin_audit_log | 管理员审计日志表 | 是 | 否 |
| 管理后台 | mini_app_config | 小程序配置表 | 否（历史未补） | 是（2026-08-23） |
| 管理后台 | admin_role | 管理员角色表 | 否（历史未补） | 否 |
| 管理后台 | admin_role_permission | 角色权限关联表 | 否（历史未补） | 否 |
| 管理后台 | admin_permission | 管理员权限码字典表 | 否（历史未补） | 否 |
| 管理后台 | admin_message_template | 管理员消息模板表 | 否 | 否 |
| 客服会话 | service_conversation | 客服会话表 | 否 | 否 |
| 客服会话 | service_message | 客服会话消息表 | 是 | 否 |
| 进销存 | warehouse | 仓库表 | 否 | 否 |
| 进销存 | location | 库位表 | 否 | 否 |
| 进销存 | inventory_real_time | 库存实时表 | 否 | 否 |
| 进销存 | inventory_flow | 库存流水表 | 是 | 否 |
| 进销存 | sales_outbound | 销售出库单表 | 否 | 否 |
| 进销存 | inventory_adjust | 库存调整单表 | 否 | 否 |
| 进销存 | batch_info | 批次信息表 | 否 | 否 |
| 进销存 | expiry_warning | 临期预警表 | 否 | 否 |
| 进销存 | expiry_warning_config | 临期预警配置表 | 否（历史未补） | 否 |
| 进销存 | supplier | 供应商表 | 否 | 否 |
| 进销存 | purchase_order | 采购单表 | 否 | 否 |
| 进销存 | purchase_item | 采购明细表 | 否 | 否 |
| 进销存 | accounts_payable | 应付账款表 | 否 | 否 |
| 进销存 | accounts_receivable | 应收账款表 | 否 | 否 |
| 进销存 | inventory_transfer | 仓间调拨单表 | 否 | 否 |
| 进销存 | inventory_check | 盘点单表 | 否 | 否 |
| 通知与广播 | notification | 通知消息表 | 否（历史未补） | 否 |
| 通知与广播 | preset_broadcast_message | 预设广播消息模板 | 否 | 否 |
| 通知与广播 | reminder_config | 日常提醒配置表 | 否 | 否 |
| 通知与广播 | broadcast_schedule | 消息广播调度表 | 否 | 否 |
| 工作台 | workbench_draft | 工作台草稿/版本/发布表 | 否 | 否 |
| 工作台 | workbench_item_blocklist | 工作台发布项禁止列表 | 否 | 否 |
| 工作台 | cron_task_execution_log | 定时任务执行日志表 | 是 | 否 |

## 三、按业务模块分类的建表语句

### 模块 1：用户体系

#### 表名 `user_account`

用户账号主表，基于微信 openid 建立。

```sql
-- 1. user_account
CREATE TABLE IF NOT EXISTS `user_account` (
  `user_id` BIGINT NOT NULL AUTO_INCREMENT,
  `openid` VARCHAR(64) NOT NULL,
  `unionid` VARCHAR(64) DEFAULT NULL,
  `session_key` VARCHAR(128) NOT NULL,
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-封禁',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `last_login_time` DATETIME DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_openid` (`openid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `user_profile`

用户资料表，含昵称、头像、邀请码等。

```sql
-- 2. user_profile
CREATE TABLE IF NOT EXISTS `user_profile` (
  `profile_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT NOT NULL,
  `nickname` VARCHAR(64) DEFAULT NULL,
  `avatar_url` VARCHAR(255) DEFAULT NULL,
  `gender` TINYINT DEFAULT 0 COMMENT '0-未知 1-男 2-女',
  `phone` VARCHAR(20) DEFAULT NULL,
  `bio` VARCHAR(255) DEFAULT NULL,
  `email` VARCHAR(128) DEFAULT NULL,
  `invite_code` VARCHAR(16) NOT NULL,
  `inviter_user_id` BIGINT DEFAULT NULL COMMENT '邀请人 user_id',
  `invited_at` DATETIME DEFAULT NULL COMMENT '填写邀请码时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`profile_id`),
  UNIQUE KEY `uk_user_id` (`user_id`),
  UNIQUE KEY `uk_invite_code` (`invite_code`),
  UNIQUE KEY `uk_email` (`email`),
  KEY `idx_inviter_user_id` (`inviter_user_id`),
  CONSTRAINT `fk_profile_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`),
  CONSTRAINT `fk_profile_inviter` FOREIGN KEY (`inviter_user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `user_email_verify`

邮箱验证码表，含尝试次数与锁定机制。

```sql
-- 2b. user_email_verify
CREATE TABLE IF NOT EXISTS `user_email_verify` (
  `verify_id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `email` VARCHAR(128) NOT NULL,
  `code_hash` VARCHAR(64) NOT NULL,
  `attempts` TINYINT NOT NULL DEFAULT 0,
  `locked_until` DATETIME DEFAULT NULL,
  `expire_at` DATETIME NOT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `last_sent_at` DATETIME NOT NULL,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`verify_id`),
  UNIQUE KEY `uk_user_id` (`user_id`),
  KEY `idx_email_sent` (`email`, `last_sent_at`),
  CONSTRAINT `fk_email_verify_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `user_email_send_log`

邮件发送流水表，仅追加不删除，豁免 is_deleted。

```sql
-- 2c. user_email_send_log（流水表，豁免 is_deleted）
CREATE TABLE IF NOT EXISTS `user_email_send_log` (
  `log_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT NOT NULL,
  `email` VARCHAR(128) NOT NULL,
  `ip_address` VARCHAR(64) DEFAULT NULL,
  `sent_at` DATETIME NOT NULL COMMENT '业务发送时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  PRIMARY KEY (`log_id`),
  KEY `idx_user_sent` (`user_id`, `sent_at`),
  KEY `idx_email_sent` (`email`, `sent_at`),
  CONSTRAINT `fk_email_send_log_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='邮件发送流水表（仅追加不删除，豁免 is_deleted）';
```

#### 表名 `user_login_log`

用户登录流水表，仅追加不删除，豁免 is_deleted。

```sql
-- 3. user_login_log（流水表，豁免 is_deleted）
CREATE TABLE IF NOT EXISTS `user_login_log` (
  `log_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT NOT NULL,
  `login_time` DATETIME NOT NULL COMMENT '业务登录时间',
  `logout_time` DATETIME DEFAULT NULL COMMENT '业务登出时间',
  `ip_address` VARCHAR(64) DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  PRIMARY KEY (`log_id`),
  KEY `idx_user_id_login_time` (`user_id`, `login_time`),
  CONSTRAINT `fk_login_log_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户登录流水表（仅追加不删除，豁免 is_deleted）';
```

#### 表名 `user_address`

用户收货地址表。

```sql
-- 8. user_address
CREATE TABLE IF NOT EXISTS `user_address` (
  `address_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT NOT NULL,
  `receiver_name` VARCHAR(64) NOT NULL,
  `receiver_phone` VARCHAR(20) NOT NULL,
  `province` VARCHAR(64) NOT NULL,
  `city` VARCHAR(64) NOT NULL,
  `district` VARCHAR(64) NOT NULL,
  `detail_address` VARCHAR(255) NOT NULL,
  `is_default` TINYINT NOT NULL DEFAULT 0,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`address_id`),
  KEY `idx_user_id` (`user_id`),
  CONSTRAINT `fk_address_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `user_browse_log`

用户浏览流水表，仅追加不删除，豁免 is_deleted。

```sql
-- 14. user_browse_log（流水表，豁免 is_deleted）
CREATE TABLE IF NOT EXISTS `user_browse_log` (
  `browse_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT NOT NULL,
  `product_id` BIGINT NOT NULL,
  `start_time` DATETIME NOT NULL COMMENT '业务浏览开始时间',
  `end_time` DATETIME DEFAULT NULL COMMENT '业务浏览结束时间',
  `action_type` TINYINT DEFAULT 0 COMMENT '0-浏览 1-加购 2-下单',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  PRIMARY KEY (`browse_id`),
  KEY `idx_user_product` (`user_id`, `product_id`),
  KEY `idx_start_time` (`start_time`),
  CONSTRAINT `fk_browse_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`),
  CONSTRAINT `fk_browse_product` FOREIGN KEY (`product_id`) REFERENCES `product` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户浏览流水表（仅追加不删除，豁免 is_deleted）';
```

#### 表名 `user_refresh_token`

用户 Refresh Token 表，通过 revoked_at 撤销，豁免 is_deleted。

```sql
-- 15. user_refresh_token（Token 豁免表，不补 is_deleted）
CREATE TABLE IF NOT EXISTS `user_refresh_token` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `token_hash` VARCHAR(128) NOT NULL,
  `expires_at` DATETIME NOT NULL,
  `revoked_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_token_hash` (`token_hash`),
  KEY `idx_user_id` (`user_id`),
  CONSTRAINT `fk_refresh_token_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户Refresh Token表（仅追加不删除，豁免 is_deleted）';
```

#### 表名 `user_message`

用户站内消息表，会话消息类，通过业务状态/归档管理，豁免 is_deleted。

```sql
-- 32. user_message (Schema v4)
CREATE TABLE IF NOT EXISTS `user_message` (
  `message_id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `category` TINYINT NOT NULL COMMENT '1-系统 2-订单 3-宠物',
  `message_type` VARCHAR(32) NOT NULL,
  `title` VARCHAR(128) NOT NULL,
  `content` VARCHAR(500) NOT NULL,
  `extra_json` TEXT,
  `biz_type` VARCHAR(32) DEFAULT NULL,
  `biz_id` BIGINT DEFAULT NULL,
  `action_path` VARCHAR(255) DEFAULT NULL,
  `sender_type` TINYINT NOT NULL DEFAULT 0 COMMENT '0-系统 1-用户',
  `sender_user_id` BIGINT DEFAULT NULL,
  `is_read` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未读 1-已读',
  `read_time` DATETIME DEFAULT NULL,
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-用户删除',
  `priority` TINYINT NOT NULL DEFAULT 0 COMMENT '0-普通 1-重要',
  `idempotency_key` VARCHAR(64) DEFAULT NULL,
  `expire_time` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  PRIMARY KEY (`message_id`),
  KEY `idx_user_category_time` (`user_id`, `category`, `created_at`),
  KEY `idx_user_read_time` (`user_id`, `is_read`, `created_at`),
  KEY `idx_user_status_time` (`user_id`, `status`, `created_at`),
  UNIQUE KEY `uk_user_idempotency` (`user_id`, `idempotency_key`),
  CONSTRAINT `fk_user_message_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`),
  CONSTRAINT `fk_user_message_sender` FOREIGN KEY (`sender_user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `user_preferences`

用户偏好设置表（宠物每日提醒等）。

```sql
-- 33. user_preferences (Schema v5)
CREATE TABLE IF NOT EXISTS `user_preferences` (
  `preference_id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `pet_daily_reminder_enabled` TINYINT NOT NULL DEFAULT 0 COMMENT '0-关闭 1-开启',
  `pet_reminder_time` VARCHAR(5) NOT NULL DEFAULT '20:00' COMMENT '每日提醒时刻 HH:mm',
  `extra_json` TEXT,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`preference_id`),
  UNIQUE KEY `uk_user_id` (`user_id`),
  KEY `idx_reminder_schedule` (`pet_daily_reminder_enabled`, `pet_reminder_time`),
  CONSTRAINT `fk_user_preferences_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `user_feedback`

用户反馈表。

```sql
-- 46. user_feedback (Schema v13)
CREATE TABLE IF NOT EXISTS `user_feedback` (
  `feedback_id` BIGINT NOT NULL AUTO_INCREMENT,
  `feedback_no` VARCHAR(32) NOT NULL COMMENT '对外编号 FB+日期+序号',
  `user_id` BIGINT NOT NULL,
  `type` VARCHAR(16) NOT NULL COMMENT 'bug/suggest/delivery/aftersale/other',
  `content` VARCHAR(500) NOT NULL,
  `image_urls` JSON DEFAULT NULL COMMENT '截图 URL 数组，最多 6 张',
  `order_id` BIGINT DEFAULT NULL,
  `contact_phone` VARCHAR(20) DEFAULT NULL,
  `status` TINYINT NOT NULL DEFAULT 10 COMMENT '10-pending 20-processing 30-resolved 40-closed',
  `admin_reply` VARCHAR(500) DEFAULT NULL,
  `processed_by` BIGINT DEFAULT NULL COMMENT '处理管理员 admin_id',
  `processed_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`feedback_id`),
  UNIQUE KEY `uk_feedback_no` (`feedback_no`),
  KEY `idx_status_create` (`status`, `created_at`),
  KEY `idx_user_create` (`user_id`, `created_at`),
  KEY `idx_order_id` (`order_id`),
  CONSTRAINT `fk_feedback_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`),
  CONSTRAINT `fk_feedback_order` FOREIGN KEY (`order_id`) REFERENCES `order_main` (`order_id`),
  CONSTRAINT `fk_feedback_admin` FOREIGN KEY (`processed_by`) REFERENCES `admin_user` (`admin_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `user_sms_log`

用户短信流水表，仅追加不删除，豁免 is_deleted。

```sql
-- 77. user_sms_log（流水表，豁免 is_deleted）
CREATE TABLE IF NOT EXISTS `user_sms_log` (
  `log_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT DEFAULT NULL COMMENT '用户ID（未登录时可为空）',
  `phone` VARCHAR(20) NOT NULL COMMENT '接收手机号',
  `sms_type` VARCHAR(32) NOT NULL COMMENT '短信类型：login/verify/notify等',
  `content` VARCHAR(500) NOT NULL COMMENT '短信内容',
  `send_status` TINYINT NOT NULL DEFAULT 0 COMMENT '发送状态：0-待发送 1-成功 2-失败',
  `fail_reason` VARCHAR(255) DEFAULT NULL COMMENT '失败原因',
  `sent_at` DATETIME DEFAULT NULL COMMENT '业务发送时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  PRIMARY KEY (`log_id`),
  KEY `idx_user_created` (`user_id`, `created_at`),
  KEY `idx_phone_created` (`phone`, `created_at`),
  KEY `idx_sms_type_created` (`sms_type`, `created_at`),
  CONSTRAINT `fk_sms_log_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户短信流水表（仅追加不删除，豁免 is_deleted）';
```

#### 表名 `user_notification_setting`

用户通知设置表。

```sql
-- 84. user_notification_setting · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `user_notification_setting` (
  `setting_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `order_push_enabled` TINYINT NOT NULL DEFAULT 1 COMMENT '订单消息推送：1-开启 0-关闭',
  `pet_reminder_enabled` TINYINT NOT NULL DEFAULT 1 COMMENT '宠物日常提醒推送：1-开启 0-关闭',
  `promotion_push_enabled` TINYINT NOT NULL DEFAULT 1 COMMENT '营销活动推送：1-开启 0-关闭',
  `system_push_enabled` TINYINT NOT NULL DEFAULT 1 COMMENT '系统公告推送：1-开启 0-关闭',
  `email_enabled` TINYINT NOT NULL DEFAULT 0 COMMENT '邮件通知：1-开启 0-关闭',
  `sms_enabled` TINYINT NOT NULL DEFAULT 0 COMMENT '短信通知：1-开启 0-关闭',
  `quiet_hours_start` VARCHAR(5) DEFAULT NULL COMMENT '免打扰开始时间 HH:mm',
  `quiet_hours_end` VARCHAR(5) DEFAULT NULL COMMENT '免打扰结束时间 HH:mm',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`setting_id`),
  UNIQUE KEY `uk_user_id` (`user_id`),
  CONSTRAINT `fk_user_notification_setting_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户通知设置表';
```

#### 表名 `user_point_balance`

用户积分余额表。

```sql
-- 85. user_point_balance · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `user_point_balance` (
  `balance_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `total_points` INT NOT NULL DEFAULT 0 COMMENT '累计积分',
  `available_points` INT NOT NULL DEFAULT 0 COMMENT '可用积分',
  `frozen_points` INT NOT NULL DEFAULT 0 COMMENT '冻结积分',
  `expiring_points` INT NOT NULL DEFAULT 0 COMMENT '即将过期积分（30天内）',
  `next_expire_date` DATE DEFAULT NULL COMMENT '下次过期日期',
  `level` INT NOT NULL DEFAULT 1 COMMENT '会员等级：1-普通 2-银卡 3-金卡 4-钻石',
  `total_spent_amount` DECIMAL(10,2) NOT NULL DEFAULT 0 COMMENT '累计消费金额',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`balance_id`),
  UNIQUE KEY `uk_user_id` (`user_id`),
  KEY `idx_level` (`level`),
  CONSTRAINT `fk_user_point_balance_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户积分余额表';
```

#### 表名 `user_subscription`

用户订阅表。

```sql
-- 86. user_subscription · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `user_subscription` (
  `subscription_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `plan_code` VARCHAR(32) NOT NULL COMMENT '订阅套餐编码：basic/pro/premium',
  `plan_name` VARCHAR(64) NOT NULL COMMENT '订阅套餐名称',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '状态：0-未订阅 1-已订阅 2-已过期 3-已取消',
  `start_date` DATE DEFAULT NULL COMMENT '订阅开始日期',
  `end_date` DATE DEFAULT NULL COMMENT '订阅结束日期',
  `renew_enabled` TINYINT NOT NULL DEFAULT 1 COMMENT '自动续费：1-开启 0-关闭',
  `last_renew_date` DATE DEFAULT NULL COMMENT '上次续费日期',
  `next_renew_date` DATE DEFAULT NULL COMMENT '下次续费日期',
  `order_id` BIGINT DEFAULT NULL COMMENT '最近一次订阅订单ID',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`subscription_id`),
  KEY `idx_user_id_status` (`user_id`, `status`),
  KEY `idx_plan_code` (`plan_code`),
  CONSTRAINT `fk_user_subscription_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户订阅表';
```

### 模块 2：商品体系

#### 表名 `product_category`

商品分类表。

```sql
-- 4. product_category
CREATE TABLE IF NOT EXISTS `product_category` (
  `category_id` INT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `parent_id` INT NOT NULL DEFAULT 0,
  `category_name` VARCHAR(64) NOT NULL,
  `sort_order` INT DEFAULT 0,
  `slug` VARCHAR(32) DEFAULT NULL,
  `icon` VARCHAR(8) DEFAULT NULL,
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '业务状态：1-正常（启用）0-停用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`category_id`),
  UNIQUE KEY `uk_slug` (`slug`),
  KEY `idx_parent_id` (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `product`

商品表，已去除 is_hidden/hidden_at/hidden_by，改用 is_deleted 软删。

```sql
-- 5. product
CREATE TABLE IF NOT EXISTS `product` (
  `product_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `category_id` INT NOT NULL,
  `product_name` VARCHAR(128) NOT NULL,
  `slug` VARCHAR(64) DEFAULT NULL,
  `thumbnail` VARCHAR(255) DEFAULT NULL,
  `intro` VARCHAR(500) DEFAULT NULL,
  `detail_desc` TEXT,
  `extra_json` TEXT,
  `original_price` DECIMAL(10,2) DEFAULT NULL,
  `sold_count` INT NOT NULL DEFAULT 0,
  `cold_chain` TINYINT NOT NULL DEFAULT 1,
  `badge` VARCHAR(32) DEFAULT NULL,
  `img_theme` VARCHAR(16) DEFAULT 'green',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '业务状态：1-上架 0-下架',
  `card_display_sku_id` BIGINT DEFAULT NULL COMMENT '商品卡片显示规格ID',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`product_id`),
  KEY `idx_category_status` (`category_id`, `status`),
  CONSTRAINT `fk_product_category` FOREIGN KEY (`category_id`) REFERENCES `product_category` (`category_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='商品表（去除 is_hidden/hidden_at/hidden_by，改用 is_deleted 软删）';
```

#### 表名 `product_sku`

商品 SKU 表，含批次管理、序列号、计价方法等进销存扩展字段。

```sql
-- 6. product_sku
CREATE TABLE IF NOT EXISTS `product_sku` (
  `sku_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `product_id` BIGINT NOT NULL,
  `spec_json` VARCHAR(255) NOT NULL,
  `sku_name` VARCHAR(64) DEFAULT NULL,
  `price` DECIMAL(10,2) NOT NULL,
  `original_price` DECIMAL(10,2) DEFAULT NULL,
  `stock` INT NOT NULL DEFAULT 0,
  `sku_image` VARCHAR(255) DEFAULT NULL,
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '业务状态：1-正常 0-停售',
  `nutrition_json` JSON DEFAULT NULL COMMENT 'SKU 结构化营养权威源（per_100g_as_fed）',
  `barcode` VARCHAR(64) DEFAULT NULL COMMENT '商品条码（进销存 v32 扩展）',
  `batch_management_enabled` BOOLEAN NOT NULL DEFAULT TRUE COMMENT '是否启用批次管理',
  `serial_number_enabled` BOOLEAN NOT NULL DEFAULT FALSE COMMENT '是否启用序列号管理',
  `shelf_life_days` INT DEFAULT NULL COMMENT '保质期天数',
  `costing_method` VARCHAR(32) NOT NULL DEFAULT 'WEIGHTED_AVERAGE' COMMENT '计价方法：WEIGHTED_AVERAGE/FIFO/SPECIFIC',
  `cost` DECIMAL(10,2) DEFAULT NULL COMMENT '成本价（可选）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`sku_id`),
  KEY `idx_product_id` (`product_id`),
  CONSTRAINT `fk_sku_product` FOREIGN KEY (`product_id`) REFERENCES `product` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 模块 3：交易订单

#### 表名 `cart`

购物车表。

```sql
-- 7. cart
CREATE TABLE IF NOT EXISTS `cart` (
  `cart_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT NOT NULL,
  `sku_id` BIGINT NOT NULL,
  `quantity` INT NOT NULL DEFAULT 1,
  `is_checked` TINYINT NOT NULL DEFAULT 1,
  `add_time` DATETIME NOT NULL COMMENT '业务加入时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`cart_id`),
  UNIQUE KEY `idx_user_sku` (`user_id`, `sku_id`),
  CONSTRAINT `fk_cart_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`),
  CONSTRAINT `fk_cart_sku` FOREIGN KEY (`sku_id`) REFERENCES `product_sku` (`sku_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `order_main`

订单主表，含 C 端用户逻辑删除 user_deleted_at 与管理员 is_deleted 隔离语义。

```sql
-- 9. order_main
CREATE TABLE IF NOT EXISTS `order_main` (
  `order_id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `out_trade_no` VARCHAR(64) DEFAULT NULL,
  `total_amount` DECIMAL(10,2) NOT NULL,
  `pay_amount` DECIMAL(10,2) DEFAULT NULL,
  `subtotal` DECIMAL(10,2) DEFAULT NULL,
  `shipping_fee` DECIMAL(10,2) DEFAULT NULL,
  `coupon_discount` DECIMAL(10,2) NOT NULL DEFAULT 0,
  `buyer_message` VARCHAR(100) DEFAULT NULL,
  `delivery_method` VARCHAR(64) DEFAULT NULL,
  `cold_chain` TINYINT NOT NULL DEFAULT 0,
  `order_status` TINYINT NOT NULL COMMENT '10-待支付 20-待发货 30-待收货 40-已完成 50-已关闭 60-售后中',
  `address_snapshot` TEXT NOT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `pay_time` DATETIME DEFAULT NULL,
  `expire_at` DATETIME DEFAULT NULL,
  `shipped_at` DATETIME DEFAULT NULL,
  `user_deleted_at` DATETIME DEFAULT NULL COMMENT 'C端用户逻辑删除时间（NULL=未删除）。与管理员 is_hidden 语义隔离，不复用',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`order_id`),
  UNIQUE KEY `uk_out_trade_no` (`out_trade_no`),
  KEY `idx_user_status_time` (`user_id`, `order_status`, `created_at`),
  KEY `idx_user_deleted_time` (`user_id`, `user_deleted_at`, `created_at`),
  CONSTRAINT `fk_order_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `order_detail`

订单明细表，含批次/出库单/库位等进销存扩展字段。

```sql
-- 10. order_detail
CREATE TABLE IF NOT EXISTS `order_detail` (
  `detail_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `order_id` BIGINT NOT NULL,
  `product_id` BIGINT NOT NULL,
  `sku_id` BIGINT NOT NULL,
  `sku_snapshot` TEXT NOT NULL COMMENT 'SKU快照（JSON：label/emoji/thumb_theme/price/original_price/spec_json/product_name/sku_image）',
  `product_name` VARCHAR(128) DEFAULT NULL,
  `product_image` VARCHAR(255) DEFAULT NULL,
  `batch_id` BIGINT DEFAULT NULL COMMENT '批次ID（Schema v32 扩展，进销存出库追溯）',
  `outbound_id` BIGINT DEFAULT NULL COMMENT '出库单ID（Schema v32 扩展，关联 sales_outbound）',
  `location_id` BIGINT DEFAULT 1 COMMENT '库位ID（Schema v32 扩展）',
  `buy_price` DECIMAL(10,2) NOT NULL,
  `buy_quantity` INT NOT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`detail_id`),
  KEY `idx_order_id` (`order_id`),
  CONSTRAINT `fk_detail_order` FOREIGN KEY (`order_id`) REFERENCES `order_main` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `payment_log`

支付流水表，仅追加不删除，豁免 is_deleted。

```sql
-- 11. payment_log（流水表，豁免 is_deleted）
CREATE TABLE IF NOT EXISTS `payment_log` (
  `payment_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `order_id` BIGINT NOT NULL,
  `pay_channel` VARCHAR(32) NOT NULL,
  `transaction_id` VARCHAR(64) NOT NULL,
  `pay_amount` DECIMAL(10,2) NOT NULL,
  `pay_status` TINYINT NOT NULL COMMENT '1-成功 0-失败',
  `pay_time` DATETIME DEFAULT NULL COMMENT '业务支付时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  PRIMARY KEY (`payment_id`),
  UNIQUE KEY `uk_transaction_id` (`transaction_id`),
  KEY `idx_order_id` (`order_id`),
  CONSTRAINT `fk_payment_order` FOREIGN KEY (`order_id`) REFERENCES `order_main` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='支付流水表（仅追加不删除，豁免 is_deleted）';
```

#### 表名 `logistics`

物流表。

```sql
-- 12. logistics
CREATE TABLE IF NOT EXISTS `logistics` (
  `logistics_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `order_id` BIGINT NOT NULL,
  `express_company` VARCHAR(64) DEFAULT NULL,
  `express_no` VARCHAR(64) DEFAULT NULL,
  `logistics_status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未发货 1-已发货 2-已签收',
  `logistics_trace` TEXT,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`logistics_id`),
  UNIQUE KEY `uk_order_id` (`order_id`),
  CONSTRAINT `fk_logistics_order` FOREIGN KEY (`order_id`) REFERENCES `order_main` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `after_sales`

售后单表（旧版），已去除 is_hidden/hidden_at/hidden_by，改用 is_deleted 软删。

```sql
-- 13. after_sales
CREATE TABLE IF NOT EXISTS `after_sales` (
  `after_sales_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `order_id` BIGINT NOT NULL,
  `refund_amount` DECIMAL(10,2) NOT NULL,
  `reason` VARCHAR(255) NOT NULL,
  `status` TINYINT NOT NULL COMMENT '业务状态：1-待审核 2-同意 3-拒绝 4-退款成功',
  `apply_time` DATETIME NOT NULL COMMENT '业务申请时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`after_sales_id`),
  KEY `idx_order_id` (`order_id`),
  CONSTRAINT `fk_after_sales_order` FOREIGN KEY (`order_id`) REFERENCES `order_main` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='售后单表（去除 is_hidden/hidden_at/hidden_by，改用 is_deleted 软删）';
```

#### 表名 `order_review`

订单评价表。

```sql
-- 34-36. order review (Schema v9)
CREATE TABLE IF NOT EXISTS `order_review` (
  `review_id` BIGINT NOT NULL AUTO_INCREMENT,
  `order_id` BIGINT NOT NULL,
  `user_id` BIGINT NOT NULL,
  `anonymous` TINYINT NOT NULL DEFAULT 0 COMMENT '0-实名 1-匿名',
  `created_at` DATETIME NOT NULL,
  PRIMARY KEY (`review_id`),
  UNIQUE KEY `uk_order_id` (`order_id`),
  KEY `idx_user_created` (`user_id`, `created_at`),
  CONSTRAINT `fk_order_review_order` FOREIGN KEY (`order_id`) REFERENCES `order_main` (`order_id`),
  CONSTRAINT `fk_order_review_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `order_review_item`

评价项表。

```sql
CREATE TABLE IF NOT EXISTS `order_review_item` (
  `item_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `review_id` BIGINT NOT NULL,
  `order_item_id` BIGINT NOT NULL,
  `product_id` BIGINT NOT NULL,
  `sku_id` BIGINT NOT NULL,
  `rating` TINYINT NOT NULL COMMENT '1-5',
  `content` VARCHAR(200) DEFAULT NULL,
  `tags` TEXT,
  `image_urls` TEXT,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`item_id`),
  KEY `idx_review_id` (`review_id`),
  KEY `idx_order_item_id` (`order_item_id`),
  CONSTRAINT `fk_order_review_item_review` FOREIGN KEY (`review_id`) REFERENCES `order_review` (`review_id`),
  CONSTRAINT `fk_order_review_item_detail` FOREIGN KEY (`order_item_id`) REFERENCES `order_detail` (`detail_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `order_review_draft`

评价草稿表。

```sql
CREATE TABLE IF NOT EXISTS `order_review_draft` (
  `draft_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `order_id` BIGINT NOT NULL,
  `user_id` BIGINT NOT NULL,
  `payload` TEXT NOT NULL,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`draft_id`),
  UNIQUE KEY `uk_order_user` (`order_id`, `user_id`),
  CONSTRAINT `fk_order_review_draft_order` FOREIGN KEY (`order_id`) REFERENCES `order_main` (`order_id`),
  CONSTRAINT `fk_order_review_draft_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `refund_record`

退款单表（BE-405~407 Mock 前置方案）。

```sql
-- 76. refund_record (Schema v43 · 退款单 BE-405~407 Mock 前置方案)
CREATE TABLE IF NOT EXISTS `refund_record` (
  `refund_id`          BIGINT         NOT NULL AUTO_INCREMENT COMMENT '主键',
  `after_sales_id`     BIGINT         NOT NULL COMMENT '关联售后单 after_sales.after_sales_id',
  `order_id`           BIGINT         NOT NULL COMMENT '关联订单 order_main.order_id',
  `out_refund_no`      VARCHAR(64)    NOT NULL COMMENT '退款商户单号，唯一（REF + 日期 + afterSalesId）',
  `transaction_id`     VARCHAR(64)    NULL COMMENT '微信支付/第三方交易号（取自 payment_log）',
  `refund_amount`      DECIMAL(10,2)  NOT NULL COMMENT '本次退款金额（元，保留 2 位小数）',
  `total_amount`       DECIMAL(10,2)  NOT NULL COMMENT '原订单实付金额（冗余，便于对账）',
  `refund_status`      TINYINT        NOT NULL DEFAULT 0 COMMENT '0 初始化 1 提交中 2 处理中 3 成功 4 失败 5 取消',
  `status_desc`        VARCHAR(255)   NULL COMMENT '状态文字描述（mock/processing/微信错误码）',
  `refund_mode`        TINYINT        NOT NULL COMMENT '0 仅标记退款（mark_only） 1 微信原路退款（wechat_refund）',
  `mock_flag`          TINYINT        NOT NULL DEFAULT 0 COMMENT '0 真实调用微信 1 mock 模式（对账标记）',
  `submit_time`        DATETIME       NULL COMMENT '首次提交退款时间',
  `success_time`       DATETIME       NULL COMMENT '退款成功时间',
  `callback_time`      DATETIME       NULL COMMENT '回调接收时间',
  `fail_reason`        VARCHAR(255)   NULL COMMENT '失败原因（微信 err_code_des 或 mock）',
  `retry_count`        TINYINT        NOT NULL DEFAULT 0 COMMENT '补偿脚本重试次数（上限 5）',
  `last_retry_time`    DATETIME       NULL COMMENT '最近一次重试时间',
  `admin_user_id`      BIGINT         NULL COMMENT '发起退款的管理员（审计联动）',
  `notify_pay_channel` VARCHAR(32)    NULL DEFAULT 'wechat' COMMENT '退款通道：wechat（暂唯一）',
  `extra_json`         TEXT           NULL COMMENT '微信响应快照与回调 payload（JSON、脱敏）',
  `created_at`         DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at`         DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted`         TINYINT        NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`refund_id`),
  UNIQUE KEY `uk_refund_record_out_refund_no` (`out_refund_no`),
  INDEX `idx_refund_record_after_sales_id` (`after_sales_id`),
  INDEX `idx_refund_record_order_id` (`order_id`),
  INDEX `idx_refund_record_refund_status` (`refund_status`),
  INDEX `idx_refund_record_transaction_id` (`transaction_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='退款单表（BE-405~407 Mock 前置）';
```

#### 表名 `after_sales_request`

售后请求表。

```sql
-- 78. after_sales_request（售后请求表）
CREATE TABLE IF NOT EXISTS `after_sales_request` (
  `request_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `order_id` BIGINT NOT NULL COMMENT '关联订单ID',
  `user_id` BIGINT NOT NULL COMMENT '申请用户ID',
  `request_type` TINYINT NOT NULL COMMENT '售后类型：1-仅退款 2-退货退款 3-换货',
  `request_reason` VARCHAR(255) NOT NULL COMMENT '申请原因',
  `description` TEXT DEFAULT NULL COMMENT '详细描述',
  `image_urls` JSON DEFAULT NULL COMMENT '凭证图片 URL 数组',
  `refund_amount` DECIMAL(10,2) NOT NULL DEFAULT 0 COMMENT '申请退款金额',
  `return_ship_fee` DECIMAL(10,2) NOT NULL DEFAULT 0 COMMENT '退货运费',
  `status` TINYINT NOT NULL DEFAULT 10 COMMENT '业务状态：10-待审核 20-审核通过 30-审核拒绝 40-用户退货中 50-商家收货 60-退款中 70-已完成 80-已关闭',
  `apply_time` DATETIME NOT NULL COMMENT '业务申请时间',
  `audit_time` DATETIME DEFAULT NULL COMMENT '审核时间',
  `audit_by` BIGINT DEFAULT NULL COMMENT '审核管理员ID',
  `audit_remark` VARCHAR(255) DEFAULT NULL COMMENT '审核备注',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`request_id`),
  KEY `idx_order_id` (`order_id`),
  KEY `idx_user_status` (`user_id`, `status`),
  KEY `idx_status_created` (`status`, `created_at`),
  CONSTRAINT `fk_asr_order` FOREIGN KEY (`order_id`) REFERENCES `order_main` (`order_id`),
  CONSTRAINT `fk_asr_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='售后请求表';
```

#### 表名 `after_sales_item`

售后明细表。

```sql
-- 79. after_sales_item（售后明细表）
CREATE TABLE IF NOT EXISTS `after_sales_item` (
  `item_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `request_id` BIGINT NOT NULL COMMENT '售后请求ID',
  `order_detail_id` BIGINT NOT NULL COMMENT '订单明细ID',
  `product_id` BIGINT NOT NULL COMMENT '商品ID',
  `sku_id` BIGINT NOT NULL COMMENT 'SKU ID',
  `sku_snapshot` TEXT DEFAULT NULL COMMENT 'SKU快照',
  `product_name` VARCHAR(128) DEFAULT NULL COMMENT '商品名称快照',
  `sku_image` VARCHAR(255) DEFAULT NULL COMMENT 'SKU图片快照',
  `quantity` INT NOT NULL DEFAULT 1 COMMENT '售后数量',
  `unit_price` DECIMAL(10,2) NOT NULL COMMENT '商品单价',
  `refund_amount` DECIMAL(10,2) NOT NULL DEFAULT 0 COMMENT '本项退款金额',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`item_id`),
  KEY `idx_request_id` (`request_id`),
  KEY `idx_order_detail_id` (`order_detail_id`),
  CONSTRAINT `fk_asi_request` FOREIGN KEY (`request_id`) REFERENCES `after_sales_request` (`request_id`),
  CONSTRAINT `fk_asi_order_detail` FOREIGN KEY (`order_detail_id`) REFERENCES `order_detail` (`detail_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='售后明细表';
```

### 模块 4：资讯内容

#### 表名 `article`

资讯表，已去除 is_hidden/hidden_at/hidden_by，改用 is_deleted 软删。

```sql
-- 16. article
CREATE TABLE IF NOT EXISTS `article` (
  `article_id` VARCHAR(32) NOT NULL COMMENT '主键（业务编码）',
  `type` VARCHAR(16) NOT NULL,
  `tag` VARCHAR(32) DEFAULT NULL,
  `title` VARCHAR(128) NOT NULL,
  `summary` VARCHAR(255) DEFAULT NULL,
  `cover_url` VARCHAR(255) DEFAULT NULL,
  `cover_theme` VARCHAR(16) DEFAULT NULL,
  `cover_emoji` VARCHAR(8) DEFAULT NULL,
  `content_json` TEXT NOT NULL,
  `position` VARCHAR(16) NOT NULL DEFAULT 'home',
  `sort_order` INT NOT NULL DEFAULT 0,
  `views` INT NOT NULL DEFAULT 0,
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '业务状态：1-已发布 0-草稿',
  `published_at` DATETIME DEFAULT NULL COMMENT '业务发布时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`article_id`),
  KEY `idx_position_status_sort` (`position`, `status`, `sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='资讯表（去除 is_hidden/hidden_at/hidden_by，改用 is_deleted 软删）';
```

#### 表名 `cms_page`

CMS 页面表（用户协议/隐私/关于/商户）。

```sql
-- 17. cms_page
CREATE TABLE IF NOT EXISTS `cms_page` (
  `page_id` INT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `type` VARCHAR(16) NOT NULL COMMENT 'user|privacy|about|merchant',
  `title` VARCHAR(128) NOT NULL,
  `content_json` TEXT NOT NULL,
  `status` TINYINT NOT NULL DEFAULT 1,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`page_id`),
  UNIQUE KEY `uk_type` (`type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `article_favorite`

资讯收藏表（BE-FAV-01）。历史表，未补 is_deleted/updated_at。

```sql
-- 75. article_favorite (Schema v39 · 资讯收藏表 BE-FAV-01)
CREATE TABLE IF NOT EXISTS `article_favorite` (
  `favorite_id` VARCHAR(32) NOT NULL COMMENT '收藏记录ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `article_id` VARCHAR(32) NOT NULL COMMENT '资讯ID',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`favorite_id`),
  INDEX `idx_article_favorite_user` (`user_id`),
  INDEX `idx_article_favorite_article` (`article_id`),
  UNIQUE KEY `uk_article_favorite_user_article` (`user_id`, `article_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='资讯收藏表（BE-FAV-01）';
```

#### 表名 `article_category`

资讯分类表。

```sql
-- 80. article_category（资讯分类表）
CREATE TABLE IF NOT EXISTS `article_category` (
  `category_id` INT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `parent_id` INT NOT NULL DEFAULT 0 COMMENT '父分类ID，0-顶级',
  `category_name` VARCHAR(64) NOT NULL COMMENT '分类名称',
  `category_code` VARCHAR(32) DEFAULT NULL COMMENT '分类编码',
  `icon` VARCHAR(8) DEFAULT NULL COMMENT '图标emoji',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序，升序',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '业务状态：1-启用 0-停用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`category_id`),
  UNIQUE KEY `uk_category_code` (`category_code`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_status_sort` (`status`, `sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='资讯分类表';
```

### 模块 5：宠物档案与记录

#### 表名 `pet`

宠物档案表，含默认所属用户、偏好、病史、过敏原等。

```sql
-- 18-24. pet profile (Schema v3)
CREATE TABLE IF NOT EXISTS `pet` (
  `pet_id` BIGINT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(64) NOT NULL,
  `avatar_url` VARCHAR(255) DEFAULT NULL,
  `breed` VARCHAR(64) DEFAULT NULL,
  `gender` TINYINT DEFAULT 0 COMMENT '性别：0-未知，1-雄性，2-雌性',
  `birthday` DATE DEFAULT NULL,
  `default_owner_user_id` BIGINT NOT NULL COMMENT '默认所属用户，不可变',
  `preferences` VARCHAR(500) DEFAULT NULL,
  `medical_history` TEXT,
  `allergens` VARCHAR(500) DEFAULT NULL,
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-已软删',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`pet_id`),
  KEY `idx_default_owner` (`default_owner_user_id`),
  KEY `idx_status_create` (`status`, `created_at`),
  CONSTRAINT `fk_pet_default_owner` FOREIGN KEY (`default_owner_user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `pet_owner`

宠物所属关系表，支持默认/共同所属。

```sql
CREATE TABLE IF NOT EXISTS `pet_owner` (
  `pet_owner_id` BIGINT NOT NULL AUTO_INCREMENT,
  `pet_id` BIGINT NOT NULL,
  `user_id` BIGINT NOT NULL,
  `ownership_type` TINYINT NOT NULL DEFAULT 1 COMMENT '1-默认所属 2-共同所属',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-有效 0-已解除',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`pet_owner_id`),
  UNIQUE KEY `uk_pet_user` (`pet_id`, `user_id`),
  KEY `idx_user_status` (`user_id`, `status`),
  CONSTRAINT `fk_pet_owner_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`),
  CONSTRAINT `fk_pet_owner_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `pet_license`

宠物证件表（狗证），含审核状态、本体/证件照片。

```sql
CREATE TABLE IF NOT EXISTS `pet_license` (
  `license_id` BIGINT NOT NULL AUTO_INCREMENT,
  `pet_id` BIGINT NOT NULL,
  `license_no` VARCHAR(64) NOT NULL,
  `license_type` VARCHAR(32) NOT NULL,
  `valid_from` DATE DEFAULT NULL,
  `valid_to` DATE DEFAULT NULL,
  `adoption_date` DATE DEFAULT NULL COMMENT '领养/入籍日期，来源于狗证证面，陪伴时长计算基准',
  `license_status` VARCHAR(32) NOT NULL,
  `photo_url` VARCHAR(255) DEFAULT NULL,
  `cert_status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-missing 1-pending 2-rejected 3-approved',
  `body_photo_urls` JSON DEFAULT NULL COMMENT '本体照片 URL 数组',
  `license_photo_url` VARCHAR(255) DEFAULT NULL COMMENT '证件照片 URL',
  `reject_reason` VARCHAR(512) DEFAULT NULL,
  `submitted_at` DATETIME DEFAULT NULL,
  `reviewed_at` DATETIME DEFAULT NULL,
  `reviewed_by` BIGINT DEFAULT NULL COMMENT '审核管理员 ID',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`license_id`),
  UNIQUE KEY `uk_pet_id` (`pet_id`),
  CONSTRAINT `fk_pet_license_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `pet_assessment`

宠物体型评估表。

```sql
CREATE TABLE IF NOT EXISTS `pet_assessment` (
  `assessment_id` BIGINT NOT NULL AUTO_INCREMENT,
  `pet_id` BIGINT NOT NULL,
  `morph_type` VARCHAR(32) NOT NULL,
  `morph_level` TINYINT NOT NULL,
  `assessed_at` DATETIME NOT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`assessment_id`),
  UNIQUE KEY `uk_pet_id` (`pet_id`),
  CONSTRAINT `fk_pet_assessment_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `pet_daily_status`

宠物每日状态表。**注意**：心情/体重/排便 3 字段已 DEPRECATED（Schema V58），迁移至 pet_mood_record/pet_stool_record/pet_daily_health_metric，兼容期保留。

```sql
CREATE TABLE IF NOT EXISTS `pet_daily_status` (
  `status_id` BIGINT NOT NULL AUTO_INCREMENT,
  `pet_id` BIGINT NOT NULL,
  `record_date` DATE NOT NULL,
  `daily_mood` VARCHAR(32) DEFAULT NULL COMMENT '-- DEPRECATED Schema V58：已迁移至 pet_mood_record.mood_value（record_time 默认 12:00:00），兼容期保留',
  `daily_weight_kg` DECIMAL(6,2) DEFAULT NULL COMMENT '当日体重 kg -- DEPRECATED Schema V58：已迁移至 pet_daily_health_metric.weight_kg，兼容期保留',
  `daily_stool_health` VARCHAR(32) DEFAULT NULL COMMENT '-- DEPRECATED Schema V58：已迁移至 pet_stool_record.stool_value（record_time 默认 12:00:00），兼容期保留',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`status_id`),
  UNIQUE KEY `uk_pet_date` (`pet_id`, `record_date`),
  CONSTRAINT `fk_pet_daily_status_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物每日状态表（心情/体重/排便 3 字段已 DEPRECATED，迁移至 3 张新表 pet_mood_record/pet_stool_record/pet_daily_health_metric）';
```

#### 表名 `pet_allergy_ingredient`

宠物过敏食材关联表（纯关联表补三字段）。

```sql
-- 82. pet_allergy_ingredient（宠物过敏食材关联表，纯关联表补三字段）
CREATE TABLE IF NOT EXISTS `pet_allergy_ingredient` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `pet_id` BIGINT NOT NULL COMMENT '宠物ID',
  `ingredient_id` BIGINT DEFAULT NULL COMMENT '食材字典ID',
  `ingredient_name` VARCHAR(64) NOT NULL COMMENT '食材名称（冗余，便于显示）',
  `severity` TINYINT NOT NULL DEFAULT 1 COMMENT '严重程度：1-轻度 2-中度 3-重度',
  `notes` VARCHAR(255) DEFAULT NULL COMMENT '备注说明',
  `created_by` BIGINT DEFAULT NULL COMMENT '创建人 user_id',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_pet_ingredient` (`pet_id`, `ingredient_id`),
  KEY `idx_ingredient_id` (`ingredient_id`),
  CONSTRAINT `fk_pai_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物过敏食材关联表';
```

#### 表名 `pet_health_record`

宠物健康档案表（疫苗/驱虫/体检/医疗/其他）。

```sql
-- 83. pet_health_record · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `pet_health_record` (
  `record_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `pet_id` BIGINT NOT NULL COMMENT '宠物ID',
  `record_type` VARCHAR(32) NOT NULL COMMENT '记录类型：疫苗/驱虫/体检/医疗/其他',
  `record_date` DATE NOT NULL COMMENT '记录日期',
  `title` VARCHAR(128) DEFAULT NULL COMMENT '记录标题',
  `content` TEXT COMMENT '记录详情',
  `hospital_name` VARCHAR(128) DEFAULT NULL COMMENT '医院/机构名称',
  `doctor_name` VARCHAR(64) DEFAULT NULL COMMENT '医生姓名',
  `next_check_date` DATE DEFAULT NULL COMMENT '下次复查日期',
  `attachments` JSON DEFAULT NULL COMMENT '附件URL数组',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`record_id`),
  KEY `idx_pet_id_record_date` (`pet_id`, `record_date`),
  KEY `idx_pet_id_record_type` (`pet_id`, `record_type`),
  CONSTRAINT `fk_pet_health_record_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物健康档案表';
```

#### 表名 `pet_breed_dict`

宠物品种字典表。

```sql
-- 87. pet_breed_dict · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `pet_breed_dict` (
  `breed_id` INT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `species` VARCHAR(16) NOT NULL COMMENT '物种：dog/cat/rabbit/other',
  `breed_code` VARCHAR(32) NOT NULL COMMENT '品种编码',
  `breed_name` VARCHAR(64) NOT NULL COMMENT '品种名称（中文）',
  `breed_name_en` VARCHAR(64) DEFAULT NULL COMMENT '品种名称（英文）',
  `origin` VARCHAR(64) DEFAULT NULL COMMENT '原产地',
  `size_category` VARCHAR(16) DEFAULT NULL COMMENT '体型分类：small/medium/large/giant',
  `life_expectancy_years` INT DEFAULT NULL COMMENT '预期寿命（年）',
  `weight_range_kg` VARCHAR(32) DEFAULT NULL COMMENT '体重范围 kg，如 10-25',
  `height_range_cm` VARCHAR(32) DEFAULT NULL COMMENT '肩高范围 cm',
  `temperament_tags` JSON DEFAULT NULL COMMENT '性格标签数组',
  `care_level` TINYINT DEFAULT NULL COMMENT '饲养难度：1-简单 2-中等 3-困难',
  `exercise_needs` TINYINT DEFAULT NULL COMMENT '运动需求：1-低 2-中 3-高',
  `grooming_needs` TINYINT DEFAULT NULL COMMENT '美容需求：1-低 2-中 3-高',
  `shedding_level` TINYINT DEFAULT NULL COMMENT '掉毛程度：1-无 2-少 3-中 4-多',
  `hypoallergenic` TINYINT DEFAULT 0 COMMENT '低过敏性：1-是 0-否',
  `description` TEXT COMMENT '品种描述',
  `avatar_url` VARCHAR(255) DEFAULT NULL COMMENT '品种头像',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序值',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1-启用 0-停用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`breed_id`),
  UNIQUE KEY `uk_breed_code` (`breed_code`),
  KEY `idx_species` (`species`),
  KEY `idx_species_size` (`species`, `size_category`),
  KEY `idx_status_sort` (`status`, `sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物品种字典表';
```

#### 表名 `pet_breed_tag_dict`

宠物品种标签字典表。

```sql
-- 88. pet_breed_tag_dict · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `pet_breed_tag_dict` (
  `tag_id` INT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `tag_code` VARCHAR(32) NOT NULL COMMENT '标签编码',
  `tag_name` VARCHAR(32) NOT NULL COMMENT '标签名称',
  `category` VARCHAR(16) NOT NULL COMMENT '分类：temperament/外观/特征/饲养',
  `icon` VARCHAR(16) DEFAULT NULL COMMENT '标签图标emoji',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序值',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1-启用 0-停用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`tag_id`),
  UNIQUE KEY `uk_tag_code` (`tag_code`),
  KEY `idx_category` (`category`),
  KEY `idx_status_sort` (`status`, `sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物品种标签字典表';
```

#### 表名 `pet_nutritional_requirement`

宠物营养需求配置表。

```sql
-- 89. pet_nutritional_requirement · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `pet_nutritional_requirement` (
  `req_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `pet_id` BIGINT NOT NULL COMMENT '宠物ID',
  `req_name` VARCHAR(64) NOT NULL COMMENT '需求名称',
  `req_type` VARCHAR(32) NOT NULL COMMENT '需求类型：weight_loss/weight_gain/maintenance/senior/puppy_kitten/pregnant/lactating/post_operative/chronic_disease',
  `target_weight_kg` DECIMAL(6,2) DEFAULT NULL COMMENT '目标体重 kg',
  `daily_calories_kcal` DECIMAL(8,2) DEFAULT NULL COMMENT '每日热量需求 kcal',
  `daily_protein_g` DECIMAL(8,2) DEFAULT NULL COMMENT '每日蛋白质需求 g',
  `daily_fat_g` DECIMAL(8,2) DEFAULT NULL COMMENT '每日脂肪需求 g',
  `daily_carbs_g` DECIMAL(8,2) DEFAULT NULL COMMENT '每日碳水化合物需求 g',
  `daily_water_ml` DECIMAL(8,2) DEFAULT NULL COMMENT '每日水分需求 ml',
  `restricted_ingredients` JSON DEFAULT NULL COMMENT '限制成分数组（过敏原等）',
  `recommended_ingredients` JSON DEFAULT NULL COMMENT '推荐成分数组',
  `supplements` JSON DEFAULT NULL COMMENT '营养补充剂数组',
  `notes` TEXT COMMENT '备注说明',
  `start_date` DATE DEFAULT NULL COMMENT '开始日期',
  `end_date` DATE DEFAULT NULL COMMENT '结束日期（周期需求）',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1-生效 0-停用',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`req_id`),
  KEY `idx_pet_id_status` (`pet_id`, `status`),
  KEY `idx_req_type` (`req_type`),
  CONSTRAINT `fk_pet_nutritional_requirement_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物营养需求配置表';
```

#### 表名 `pet_mood_record`

宠物心情记录表（Schema V58，每日多条，≤5 条）。

```sql
-- 104. pet_mood_record (Schema V58 · 宠物页 UI 整改与数据重构 · 心情记录表，每日多条)
CREATE TABLE IF NOT EXISTS `pet_mood_record` (
  `mood_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '心情记录主键',
  `pet_id` BIGINT NOT NULL COMMENT '宠物ID',
  `record_date` DATE NOT NULL COMMENT '记录日期',
  `record_time` TIME NOT NULL COMMENT '记录时间 HH:mm:ss',
  `mood_value` VARCHAR(32) NOT NULL COMMENT '心情描述，如开心/无聊/兴奋',
  `mood_icon` VARCHAR(16) DEFAULT NULL COMMENT '心情图标 emoji',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除 1-已删除',
  PRIMARY KEY (`mood_id`),
  KEY `idx_pet_date` (`pet_id`, `record_date`),
  CONSTRAINT `fk_mood_record_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物心情记录表（每日多条，≤5条）';
```

#### 表名 `pet_stool_record`

宠物排便记录表（Schema V58，每日多条，含图片）。

```sql
-- 105. pet_stool_record (Schema V58 · 宠物页 UI 整改与数据重构 · 排便记录表，每日多条，含图片)
CREATE TABLE IF NOT EXISTS `pet_stool_record` (
  `stool_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '排便记录主键',
  `pet_id` BIGINT NOT NULL COMMENT '宠物ID',
  `record_date` DATE NOT NULL COMMENT '记录日期',
  `record_time` TIME NOT NULL COMMENT '记录时间 HH:mm:ss',
  `stool_value` VARCHAR(32) NOT NULL COMMENT '排便状态描述，如完美成型/偏软',
  `image_url` VARCHAR(255) DEFAULT NULL COMMENT '图片URL（可选）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除 1-已删除',
  PRIMARY KEY (`stool_id`),
  KEY `idx_pet_date` (`pet_id`, `record_date`),
  CONSTRAINT `fk_stool_record_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物排便记录表（每日多条，≤5条，含图片）';
```

#### 表名 `pet_daily_health_metric`

宠物每日健康指标表（Schema V58，体重+生理状态+卡路里目标+运动目标，按日存储历史，每宠每天 1 条唯一）。

```sql
-- 106. pet_daily_health_metric (Schema V58 · 宠物页 UI 整改与数据重构 · 每日健康指标表，按日存储)
CREATE TABLE IF NOT EXISTS `pet_daily_health_metric` (
  `metric_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '每日健康指标主键',
  `pet_id` BIGINT NOT NULL COMMENT '宠物ID',
  `record_date` DATE NOT NULL COMMENT '记录日期',
  `weight_kg` DECIMAL(6,2) DEFAULT NULL COMMENT '体重 kg',
  `physiology_status` VARCHAR(32) DEFAULT NULL COMMENT '生理状态预设值文本（正常/生病/怀孕/术后/虚弱/发情期/残疾）',
  `calorie_theory_kcal` DECIMAL(8,2) DEFAULT NULL COMMENT '卡路里理论参考值K0（系统计算，本期占位）',
  `calorie_custom_kcal` DECIMAL(8,2) DEFAULT NULL COMMENT '卡路里用户自定义K1',
  `exercise_theory_min` SMALLINT DEFAULT NULL COMMENT '运动时长理论参考值T0（系统计算，本期占位，分钟）',
  `exercise_custom_min` SMALLINT DEFAULT NULL COMMENT '运动时长用户自定义T1（分钟）',
  `user_pref_c1` DECIMAL(6,2) DEFAULT NULL COMMENT '用户偏好参数C1（参与K0计算，本期占位）',
  `user_pref_c2` DECIMAL(6,2) DEFAULT NULL COMMENT '用户偏好参数C2（参与T0计算，本期占位）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除 1-已删除',
  PRIMARY KEY (`metric_id`),
  UNIQUE KEY `uk_pet_date` (`pet_id`, `record_date`),
  CONSTRAINT `fk_health_metric_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物每日健康指标表（体重+生理状态+卡路里目标+运动目标，按日存储历史，每宠每天1条唯一）';
```

### 模块 6：餐食与运动记录

#### 表名 `pet_meal_session`

餐食会话表（Schema v51 替代原 pet_meal_session_pet 关联表）。

```sql
CREATE TABLE IF NOT EXISTS `pet_meal_session` (
  `session_id` BIGINT NOT NULL AUTO_INCREMENT,
  `pet_id` BIGINT NOT NULL COMMENT '宠物ID（Schema v51，替代原 pet_meal_session_pet 关联表）',
  `record_date` DATE NOT NULL COMMENT '冗余，便于按宠按日查询（Schema v23）',
  `period` VARCHAR(16) NOT NULL COMMENT '早上/下午/晚上',
  `meal_time` TIME DEFAULT NULL,
  `post_meal_status` VARCHAR(32) DEFAULT NULL,
  `nutrition_reference` TEXT,
  `remark` VARCHAR(500) DEFAULT NULL,
  `sort_order` TINYINT NOT NULL DEFAULT 0,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`session_id`),
  KEY `idx_meal_record_date` (`record_date`),
  KEY `idx_meal_pet_date` (`pet_id`, `record_date`),
  CONSTRAINT `fk_meal_session_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `pet_meal_item`

餐食项表（单项食物营养快照）。

```sql
CREATE TABLE IF NOT EXISTS `pet_meal_item` (
  `item_id` BIGINT NOT NULL AUTO_INCREMENT,
  `session_id` BIGINT NOT NULL,
  `food_name` VARCHAR(128) NOT NULL,
  `amount` VARCHAR(32) NOT NULL COMMENT '克重如 150g',
  `nutrition_ref` VARCHAR(256) NOT NULL DEFAULT '' COMMENT '单项食物营养参考自由文本',
  `nutrition_json` JSON DEFAULT NULL COMMENT '单项食物结构化营养快照',
  `nutrition_source` VARCHAR(16) NOT NULL DEFAULT '' COMMENT 'scan_sku|manual|legacy_text',
  `sort_order` TINYINT NOT NULL DEFAULT 0,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`item_id`),
  KEY `idx_session_id` (`session_id`),
  CONSTRAINT `fk_meal_item_session` FOREIGN KEY (`session_id`) REFERENCES `pet_meal_session` (`session_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `pet_meal_template`

餐食模板表（Schema v7，含生成列 active_template_name）。

```sql
-- 24b. pet meal templates (Schema v7)
CREATE TABLE IF NOT EXISTS `pet_meal_template` (
  `template_id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `template_name` VARCHAR(16) NOT NULL,
  `session_slot` VARCHAR(16) NOT NULL,
  `session_time` VARCHAR(5) DEFAULT NULL,
  `post_meal_status` VARCHAR(32) DEFAULT NULL,
  `nutrition_ref` VARCHAR(256) DEFAULT NULL,
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-软删',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `active_template_name` VARCHAR(16) GENERATED ALWAYS AS (CASE WHEN `status` = 1 THEN `template_name` ELSE NULL END) STORED,
  PRIMARY KEY (`template_id`),
  UNIQUE KEY `uk_user_active_template_name` (`user_id`, `active_template_name`),
  KEY `idx_user_updated` (`user_id`, `updated_at`),
  CONSTRAINT `fk_meal_template_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `pet_meal_template_item`

餐食模板项表。

```sql
CREATE TABLE IF NOT EXISTS `pet_meal_template_item` (
  `item_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `template_id` BIGINT NOT NULL,
  `food_name` VARCHAR(32) NOT NULL,
  `amount` VARCHAR(16) DEFAULT NULL,
  `nutrition_ref` VARCHAR(256) NOT NULL DEFAULT '' COMMENT '单项食物营养参考自由文本',
  `nutrition_json` JSON DEFAULT NULL COMMENT '模板项结构化营养',
  `nutrition_source` VARCHAR(16) NOT NULL DEFAULT '' COMMENT 'scan_sku|manual|legacy_text',
  `sort_order` INT NOT NULL DEFAULT 0,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`item_id`),
  KEY `idx_template_sort` (`template_id`, `sort_order`),
  CONSTRAINT `fk_meal_template_item_template` FOREIGN KEY (`template_id`) REFERENCES `pet_meal_template` (`template_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `pet_meal_template_meal`

模板-用餐日期关联表。

```sql
-- 91. pet_meal_template_meal · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `pet_meal_template_meal` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `template_id` BIGINT NOT NULL COMMENT '模板ID',
  `meal_date` DATE NOT NULL COMMENT '用餐日期',
  `period` VARCHAR(16) NOT NULL COMMENT '时段：早上/下午/晚上/夜宵',
  `sort_order` TINYINT NOT NULL DEFAULT 0 COMMENT '当日排序',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`id`),
  KEY `idx_template_id_date` (`template_id`, `meal_date`),
  CONSTRAINT `fk_pet_meal_template_meal_template` FOREIGN KEY (`template_id`) REFERENCES `pet_meal_template` (`template_id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='模板-用餐日期关联表';
```

#### 表名 `pet_meal_formula_item`

宠物食谱配方明细表。

```sql
-- 92. pet_meal_formula_item · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `pet_meal_formula_item` (
  `item_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `formula_id` BIGINT NOT NULL COMMENT '配方ID',
  `sku_id` BIGINT DEFAULT NULL COMMENT '关联商品SKU ID（可选）',
  `food_name` VARCHAR(64) NOT NULL COMMENT '食物名称',
  `food_category` VARCHAR(32) DEFAULT NULL COMMENT '食物分类：主食/辅食/零食/营养品/补充剂',
  `amount_g` DECIMAL(8,2) NOT NULL COMMENT '用量 g',
  `unit` VARCHAR(16) DEFAULT 'g' COMMENT '单位：g/ml/piece/勺',
  `calories_kcal` DECIMAL(8,2) DEFAULT NULL COMMENT '热量 kcal',
  `protein_g` DECIMAL(8,2) DEFAULT NULL COMMENT '蛋白质 g',
  `fat_g` DECIMAL(8,2) DEFAULT NULL COMMENT '脂肪 g',
  `carbs_g` DECIMAL(8,2) DEFAULT NULL COMMENT '碳水化合物 g',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '项内排序',
  `notes` VARCHAR(255) DEFAULT NULL COMMENT '备注',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`item_id`),
  KEY `idx_formula_id_sort` (`formula_id`, `sort_order`),
  KEY `idx_sku_id` (`sku_id`),
  CONSTRAINT `fk_pet_meal_formula_item_sku` FOREIGN KEY (`sku_id`) REFERENCES `product_sku` (`sku_id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物食谱配方明细表';
```

#### 表名 `pet_meal_plan`

宠物喂食计划表。

```sql
-- 90. pet_meal_plan · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `pet_meal_plan` (
  `plan_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `pet_id` BIGINT NOT NULL COMMENT '宠物ID',
  `user_id` BIGINT NOT NULL COMMENT '创建用户ID',
  `plan_name` VARCHAR(64) NOT NULL COMMENT '计划名称',
  `plan_type` VARCHAR(32) NOT NULL COMMENT '计划类型：weekly/monthly/custom',
  `start_date` DATE NOT NULL COMMENT '计划开始日期',
  `end_date` DATE DEFAULT NULL COMMENT '计划结束日期',
  `daily_meals` TINYINT NOT NULL DEFAULT 3 COMMENT '每日餐数：2/3/4',
  `calorie_target_kcal` DECIMAL(8,2) DEFAULT NULL COMMENT '每日热量目标 kcal',
  `protein_target_g` DECIMAL(8,2) DEFAULT NULL COMMENT '每日蛋白质目标 g',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-草稿 1-进行中 2-已完成 3-已暂停 4-已取消',
  `progress_percent` TINYINT NOT NULL DEFAULT 0 COMMENT '完成进度 0-100',
  `notes` VARCHAR(500) DEFAULT NULL COMMENT '备注',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`plan_id`),
  KEY `idx_pet_id_status` (`pet_id`, `status`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_start_date` (`start_date`),
  CONSTRAINT `fk_pet_meal_plan_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`),
  CONSTRAINT `fk_pet_meal_plan_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物喂食计划表';
```

#### 表名 `pet_exercise_session`

运动会话表（Schema v21 · EX-v1）。

```sql
-- 24c. pet exercise records & templates (Schema v21 · EX-v1)
CREATE TABLE IF NOT EXISTS `pet_exercise_session` (
  `session_id` BIGINT NOT NULL AUTO_INCREMENT,
  `pet_id` BIGINT NOT NULL COMMENT '冗余，便于按宠按日查询',
  `record_date` DATE NOT NULL COMMENT '冗余，便于按宠按日查询',
  `location` VARCHAR(32) NOT NULL DEFAULT '' COMMENT '运动地点 0~32 字；空串表示用户未填写（选填）',
  `start_time` TIME NOT NULL COMMENT '开始时间 HH:mm',
  `duration_minutes` SMALLINT NOT NULL COMMENT '时长 1~480 分钟',
  `assessment` VARCHAR(32) NOT NULL DEFAULT '' COMMENT '运动评估 ≤32 字',
  `ext_json` JSON DEFAULT NULL COMMENT '远期扩展',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`session_id`),
  KEY `idx_pet_date` (`pet_id`, `record_date`),
  CONSTRAINT `fk_exercise_session_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `pet_exercise_template`

运动模板表（含生成列 active_template_name）。

```sql
CREATE TABLE IF NOT EXISTS `pet_exercise_template` (
  `template_id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `template_name` VARCHAR(16) NOT NULL,
  `location` VARCHAR(32) NOT NULL DEFAULT '' COMMENT '运动地点 0~32 字；空串表示用户未填写（选填）',
  `start_time` TIME NOT NULL COMMENT '模板默认开始时间',
  `duration_minutes` SMALLINT NOT NULL,
  `assessment` VARCHAR(32) NOT NULL DEFAULT '',
  `preview_text` VARCHAR(64) NOT NULL DEFAULT '' COMMENT '如：小区 · 20 分钟',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-软删',
  `created_at` DATETIME NOT NULL,
  `updated_at` DATETIME DEFAULT NULL,
  `active_template_name` VARCHAR(16) GENERATED ALWAYS AS (CASE WHEN `status` = 1 THEN `template_name` ELSE NULL END) STORED,
  PRIMARY KEY (`template_id`),
  UNIQUE KEY `uk_user_active_exercise_template_name` (`user_id`, `active_template_name`),
  KEY `idx_user_status` (`user_id`, `status`),
  KEY `idx_user_updated` (`user_id`, `updated_at`),
  CONSTRAINT `fk_exercise_template_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `pet_exercise_plan`

宠物运动计划表。

```sql
-- 93. pet_exercise_plan · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `pet_exercise_plan` (
  `plan_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `pet_id` BIGINT NOT NULL COMMENT '宠物ID',
  `user_id` BIGINT NOT NULL COMMENT '创建用户ID',
  `plan_name` VARCHAR(64) NOT NULL COMMENT '计划名称',
  `plan_type` VARCHAR(32) NOT NULL COMMENT '计划类型：weight_loss/fitness/maintenance/recovery/weekly/custom',
  `start_date` DATE NOT NULL COMMENT '开始日期',
  `end_date` DATE DEFAULT NULL COMMENT '结束日期',
  `weekly_frequency` TINYINT NOT NULL DEFAULT 3 COMMENT '每周频次（1-7天）',
  `target_duration_minutes` SMALLINT DEFAULT NULL COMMENT '每次目标时长 分钟',
  `target_distance_km` DECIMAL(6,2) DEFAULT NULL COMMENT '每次目标距离 km',
  `target_calories_kcal` DECIMAL(8,2) DEFAULT NULL COMMENT '每次目标消耗 kcal',
  `recommended_activities` JSON DEFAULT NULL COMMENT '推荐运动类型数组',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-草稿 1-进行中 2-已完成 3-已暂停 4-已取消',
  `progress_percent` TINYINT NOT NULL DEFAULT 0 COMMENT '完成进度 0-100',
  `notes` VARCHAR(500) DEFAULT NULL COMMENT '备注',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`plan_id`),
  KEY `idx_pet_id_status` (`pet_id`, `status`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_start_date` (`start_date`),
  CONSTRAINT `fk_pet_exercise_plan_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`),
  CONSTRAINT `fk_pet_exercise_plan_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物运动计划表';
```

### 模块 7：营养因子

#### 表名 `nutrition_extra_dict`

额外营养素字典（营养配置表，Schema v20 · NUT-v1 · v52 补 is_deleted + 唯一键收敛）。

```sql
-- 50. nutrition_extra_dict (Schema v20 · NUT-v1 · v52 补 is_deleted + 唯一键收敛)
CREATE TABLE IF NOT EXISTS `nutrition_extra_dict` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `code` VARCHAR(32) NOT NULL COMMENT '字典键，如 ca / vit_a',
  `name` VARCHAR(64) NOT NULL COMMENT '展示名称',
  `default_unit` VARCHAR(16) NOT NULL DEFAULT 'g' COMMENT '建议默认单位',
  `sort` INT NOT NULL DEFAULT 0 COMMENT '排序，升序',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-启用 0-停用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未删除；1-已逻辑删除（Schema v52）',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code_del` (`code`, `is_deleted`),
  KEY `idx_status_sort` (`status`, `sort`),
  KEY `idx_dict_is_deleted` (`is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='额外营养素字典（营养配置表）';
```

#### 表名 `reminder_tag_dict`

提醒标签字典。**@deprecated 2026-08-20**：预设提醒标签改为小程序端静态内置，本表保留用于历史查询/低版本 fallback/紧急回滚，不参与新业务。

```sql
-- 55. reminder_tag_dict (Schema v28 · 日常提醒标签字典 BE-RT01)
/*
 * @deprecated 2026-08-20 · 预设提醒标签已改为小程序端静态内置（pet-app/src/constants/reminderTag.js）
 * 本表保留用于：① 历史审计与追溯；② 2026-09-20 23:59:59 前低版本小程序 fallback 调用 GET /reminder-tag-dict 读取；
 * ③ 紧急回滚场景（解除管理端路由注释即可恢复配置入口）。
 * 不参与任何新业务流程；任何写入管理端 CRUD 的尝试在 Phase 3 已从路由层拦截（返回 404），
 * Service 层写操作会输出 warn 级别弃用警告日志。
 * 物理删除提案窗口：若 2026-11-18 前（@deprecated 后 90 天）仍无历史读取需求，
 * 启动「DROP TABLE reminder_tag_dict」专项评审并在 pet-app-common 备案。
 */
CREATE TABLE IF NOT EXISTS `reminder_tag_dict` (
  `id` INTEGER PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(8) NOT NULL COMMENT '标签名称（1-6字）',
  `category` VARCHAR(16) NOT NULL DEFAULT '其他' COMMENT '分类：医疗健康/日常护理/饮食喂养/外出活动/美容造型/其他',
  `sort` INTEGER NOT NULL DEFAULT 0 COMMENT '排序值（数值越小越靠前）',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  UNIQUE KEY `uk_name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='提醒标签字典 · @deprecated 2026-08-20 · 预设标签改为小程序端内置，本表保留用于历史查询，不参与新业务';
```

#### 表名 `pet_nutrition_factor`

宠物营养因子（矩阵行，Schema v53 · 信息注册台）。

```sql
-- 97. pet_nutrition_factor (Schema v53 · 信息注册台 · 宠物营养因子矩阵行)
CREATE TABLE IF NOT EXISTS `pet_nutrition_factor` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `factor_code` VARCHAR(32) NOT NULL COMMENT '因子编码（snake_case）',
  `factor_name` VARCHAR(64) NOT NULL COMMENT '因子名称（如蛋白质占比）',
  `unit` VARCHAR(16) NOT NULL DEFAULT '%' COMMENT '单位',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-启用；0-禁用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未删除；1-已逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_factor_code_del` (`factor_code`, `is_deleted`),
  KEY `idx_status_sort` (`status`, `sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物营养因子（矩阵行）';
```

#### 表名 `pet_nutrition_factor_dimension`

宠物营养因子维度（矩阵列，Schema v53 · 信息注册台）。

```sql
-- 98. pet_nutrition_factor_dimension (Schema v53 · 信息注册台 · 宠物营养因子矩阵列)
CREATE TABLE IF NOT EXISTS `pet_nutrition_factor_dimension` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `dim_code` VARCHAR(32) NOT NULL COMMENT '维度编码（snake_case，如 infancy/growth/menstruation/obesity/pregnancy）',
  `dim_name` VARCHAR(64) NOT NULL COMMENT '维度名称（如幼年期）',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-启用；0-禁用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未删除；1-已逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_dim_code_del` (`dim_code`, `is_deleted`),
  KEY `idx_dim_status_sort` (`status`, `sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物营养因子维度（矩阵列）';
```

#### 表名 `pet_nutrition_factor_value`

宠物营养因子矩阵单元格值（Schema v53 · 信息注册台）。

```sql
-- 99. pet_nutrition_factor_value (Schema v53 · 信息注册台 · 宠物营养因子矩阵单元格值)
CREATE TABLE IF NOT EXISTS `pet_nutrition_factor_value` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `factor_id` BIGINT NOT NULL COMMENT '关联 pet_nutrition_factor.id',
  `dimension_id` BIGINT NOT NULL COMMENT '关联 pet_nutrition_factor_dimension.id',
  `factor_value` DECIMAL(8,2) NULL COMMENT '因子值（百分比，如 26.00 表示 26%）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未删除；1-已逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_factor_dim_del` (`factor_id`, `dimension_id`, `is_deleted`),
  KEY `idx_value_factor_id` (`factor_id`),
  KEY `idx_value_dimension_id` (`dimension_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='宠物营养因子矩阵单元格值';
```

### 模块 8：家庭组

#### 表名 `family_group`

家庭组表（Schema v3）。

```sql
-- 25-31. family group (Schema v3)
CREATE TABLE IF NOT EXISTS `family_group` (
  `family_group_id` BIGINT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(64) NOT NULL,
  `creator_user_id` BIGINT NOT NULL,
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-已解散',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `dissolved_at` DATETIME DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`family_group_id`),
  KEY `idx_creator_status` (`creator_user_id`, `status`),
  CONSTRAINT `fk_family_group_creator` FOREIGN KEY (`creator_user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `family_group_member`

家庭组成员表。

```sql
CREATE TABLE IF NOT EXISTS `family_group_member` (
  `member_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `family_group_id` BIGINT NOT NULL,
  `user_id` BIGINT NOT NULL,
  `member_role` TINYINT NOT NULL COMMENT '1-创建者 2-成员',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '业务状态：1-在组 0-已退出/被移除',
  `can_invite` TINYINT NOT NULL DEFAULT 0,
  `can_remove_member` TINYINT NOT NULL DEFAULT 0,
  `can_manage_member_permission` TINYINT NOT NULL DEFAULT 0,
  `join_time` DATETIME NOT NULL COMMENT '业务加入时间',
  `leave_time` DATETIME DEFAULT NULL COMMENT '业务离开时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`member_id`),
  UNIQUE KEY `uk_group_user` (`family_group_id`, `user_id`),
  CONSTRAINT `fk_fg_member_group` FOREIGN KEY (`family_group_id`) REFERENCES `family_group` (`family_group_id`),
  CONSTRAINT `fk_fg_member_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `family_group_pet`

家庭组宠物表。

```sql
CREATE TABLE IF NOT EXISTS `family_group_pet` (
  `group_pet_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `family_group_id` BIGINT NOT NULL,
  `pet_id` BIGINT NOT NULL,
  `added_by_user_id` BIGINT NOT NULL,
  `manager_user_id` BIGINT NOT NULL,
  `join_source` TINYINT NOT NULL COMMENT '1-创建者自动 2-成员自选',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '业务状态：1-在组 0-已出组',
  `join_time` DATETIME NOT NULL COMMENT '业务加入时间',
  `leave_time` DATETIME DEFAULT NULL COMMENT '业务离开时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`group_pet_id`),
  UNIQUE KEY `uk_group_pet` (`family_group_id`, `pet_id`),
  KEY `idx_pet_status` (`pet_id`, `status`),
  CONSTRAINT `fk_fg_pet_group` FOREIGN KEY (`family_group_id`) REFERENCES `family_group` (`family_group_id`),
  CONSTRAINT `fk_fg_pet_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`),
  CONSTRAINT `fk_fg_pet_added_by` FOREIGN KEY (`added_by_user_id`) REFERENCES `user_account` (`user_id`),
  CONSTRAINT `fk_fg_pet_manager` FOREIGN KEY (`manager_user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `family_group_invitation`

家庭组邀请表。

```sql
CREATE TABLE IF NOT EXISTS `family_group_invitation` (
  `invitation_id` BIGINT NOT NULL AUTO_INCREMENT,
  `family_group_id` BIGINT NOT NULL,
  `inviter_user_id` BIGINT NOT NULL,
  `invitee_user_id` BIGINT DEFAULT NULL,
  `invite_code` VARCHAR(32) DEFAULT NULL,
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-待接受 1-已接受 2-已拒绝 3-已过期 4-已撤销',
  `expire_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `respond_time` DATETIME DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`invitation_id`),
  KEY `idx_group_status` (`family_group_id`, `status`),
  CONSTRAINT `fk_fg_invite_group` FOREIGN KEY (`family_group_id`) REFERENCES `family_group` (`family_group_id`),
  CONSTRAINT `fk_fg_invite_inviter` FOREIGN KEY (`inviter_user_id`) REFERENCES `user_account` (`user_id`),
  CONSTRAINT `fk_fg_invite_invitee` FOREIGN KEY (`invitee_user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `family_group_member_removal`

家庭组成员移除表。

```sql
CREATE TABLE IF NOT EXISTS `family_group_member_removal` (
  `removal_id` BIGINT NOT NULL AUTO_INCREMENT,
  `family_group_id` BIGINT NOT NULL,
  `target_user_id` BIGINT NOT NULL,
  `initiator_user_id` BIGINT NOT NULL,
  `pets_exit_choice` TINYINT NOT NULL DEFAULT 0 COMMENT '0-待确认 1-宠物留组 2-宠物随成员出组',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-待确认 1-已完成 2-已取消',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `resolve_time` DATETIME DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`removal_id`),
  KEY `idx_group_target` (`family_group_id`, `target_user_id`),
  CONSTRAINT `fk_fg_member_removal_group` FOREIGN KEY (`family_group_id`) REFERENCES `family_group` (`family_group_id`),
  CONSTRAINT `fk_fg_member_removal_target` FOREIGN KEY (`target_user_id`) REFERENCES `user_account` (`user_id`),
  CONSTRAINT `fk_fg_member_removal_initiator` FOREIGN KEY (`initiator_user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `family_group_pet_removal_request`

家庭组宠物移除申请表。

```sql
CREATE TABLE IF NOT EXISTS `family_group_pet_removal_request` (
  `request_id` BIGINT NOT NULL AUTO_INCREMENT,
  `family_group_id` BIGINT NOT NULL,
  `group_pet_id` BIGINT NOT NULL,
  `pet_id` BIGINT NOT NULL,
  `requester_user_id` BIGINT NOT NULL,
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-表决中 1-已通过 2-已驳回 3-已撤销 4-已执行',
  `reason` VARCHAR(255) DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `resolve_time` DATETIME DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`request_id`),
  KEY `idx_group_pet_status` (`family_group_id`, `pet_id`, `status`),
  CONSTRAINT `fk_fg_pet_removal_group` FOREIGN KEY (`family_group_id`) REFERENCES `family_group` (`family_group_id`),
  CONSTRAINT `fk_fg_pet_removal_group_pet` FOREIGN KEY (`group_pet_id`) REFERENCES `family_group_pet` (`group_pet_id`),
  CONSTRAINT `fk_fg_pet_removal_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`),
  CONSTRAINT `fk_fg_pet_removal_requester` FOREIGN KEY (`requester_user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `family_group_pet_removal_vote`

家庭组宠物移除投票表。

```sql
CREATE TABLE IF NOT EXISTS `family_group_pet_removal_vote` (
  `vote_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `request_id` BIGINT NOT NULL,
  `voter_user_id` BIGINT NOT NULL,
  `vote_status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-待表决 1-同意 2-拒绝',
  `vote_time` DATETIME DEFAULT NULL COMMENT '业务表决时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`vote_id`),
  UNIQUE KEY `uk_request_voter` (`request_id`, `voter_user_id`),
  CONSTRAINT `fk_fg_pet_removal_vote_request` FOREIGN KEY (`request_id`) REFERENCES `family_group_pet_removal_request` (`request_id`),
  CONSTRAINT `fk_fg_pet_removal_vote_user` FOREIGN KEY (`voter_user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `family_group_tag`

家庭组标签关联表（纯关联表补三字段）。

```sql
-- 81. family_group_tag（家庭组标签关联表，纯关联表补三字段）
CREATE TABLE IF NOT EXISTS `family_group_tag` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `family_group_id` BIGINT NOT NULL COMMENT '家庭组ID',
  `tag_id` BIGINT NOT NULL COMMENT '标签ID',
  `created_by` BIGINT DEFAULT NULL COMMENT '创建人 user_id',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_group_tag` (`family_group_id`, `tag_id`),
  KEY `idx_tag_id` (`tag_id`),
  CONSTRAINT `fk_fgt_group` FOREIGN KEY (`family_group_id`) REFERENCES `family_group` (`family_group_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='家庭组标签关联表';
```

### 模块 9：管理后台

#### 表名 `admin_user`

管理员用户表（Schema v10，v40 增加 role_id）。

```sql
-- 41-45. admin / shop_config (Schema v10, v40 增加 role_id)
CREATE TABLE IF NOT EXISTS `admin_user` (
  `admin_id` BIGINT NOT NULL AUTO_INCREMENT,
  `username` VARCHAR(64) NOT NULL,
  `password_hash` VARCHAR(128) NOT NULL,
  `openid` VARCHAR(64) DEFAULT NULL COMMENT '二期小程序隐藏入口绑定',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-禁用',
  `role_id` BIGINT DEFAULT NULL COMMENT '角色ID（BE-P1-03 权限体系）',
  `last_login_time` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`admin_id`),
  UNIQUE KEY `uk_username` (`username`),
  KEY `idx_admin_user_role_id` (`role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

补充：Schema v40 在 admin_role 创建后为 admin_user 追加外键约束：

```sql
-- v40: admin_user 添加外键约束（admin_role 创建后）
ALTER TABLE `admin_user` ADD CONSTRAINT `fk_admin_user_role` FOREIGN KEY (`role_id`) REFERENCES `admin_role` (`role_id`) ON DELETE SET NULL ON UPDATE CASCADE;
```

#### 表名 `admin_refresh_token`

管理员 Refresh Token 表，通过 revoked_at 撤销，豁免 is_deleted。

```sql
CREATE TABLE IF NOT EXISTS `admin_refresh_token` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `admin_id` BIGINT NOT NULL,
  `token_hash` VARCHAR(128) NOT NULL,
  `expires_at` DATETIME NOT NULL,
  `revoked_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_token_hash` (`token_hash`),
  KEY `idx_admin_id` (`admin_id`),
  CONSTRAINT `fk_admin_refresh_token_admin` FOREIGN KEY (`admin_id`) REFERENCES `admin_user` (`admin_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `shop_config`

店铺配置表。

```sql
CREATE TABLE IF NOT EXISTS `shop_config` (
  `config_key` VARCHAR(64) NOT NULL COMMENT '主键',
  `config_value` TEXT,
  `updated_by` BIGINT DEFAULT NULL,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`config_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `admin_message_job`

管理员消息任务表。

```sql
CREATE TABLE IF NOT EXISTS `admin_message_job` (
  `job_id` BIGINT NOT NULL AUTO_INCREMENT,
  `target_type` VARCHAR(32) NOT NULL COMMENT 'all / users',
  `target_value` TEXT COMMENT 'JSON: user_id 列表等',
  `title` VARCHAR(128) NOT NULL,
  `content` VARCHAR(500) NOT NULL,
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-待执行 1-执行中 2-完成 3-失败',
  `sent_count` INT NOT NULL DEFAULT 0,
  `total_count` INT NOT NULL DEFAULT 0,
  `error_message` TEXT,
  `created_by` BIGINT NOT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `finish_time` DATETIME DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`job_id`),
  KEY `idx_status_create` (`status`, `created_at`),
  CONSTRAINT `fk_admin_message_job_admin` FOREIGN KEY (`created_by`) REFERENCES `admin_user` (`admin_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `admin_audit_log`

管理员审计日志表，仅追加不删除，豁免 is_deleted。

```sql
CREATE TABLE IF NOT EXISTS `admin_audit_log` (
  `log_id` BIGINT NOT NULL AUTO_INCREMENT,
  `admin_user_id` BIGINT NOT NULL,
  `action` VARCHAR(64) NOT NULL,
  `resource_type` VARCHAR(32) DEFAULT NULL,
  `resource_id` VARCHAR(64) DEFAULT NULL,
  `detail_json` TEXT,
  `ip_address` VARCHAR(45) DEFAULT NULL COMMENT '操作者 IP 地址（BE-P1-01）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  PRIMARY KEY (`log_id`),
  KEY `idx_admin_time` (`admin_user_id`, `created_at`),
  CONSTRAINT `fk_admin_audit_log_admin` FOREIGN KEY (`admin_user_id`) REFERENCES `admin_user` (`admin_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `mini_app_config`

小程序配置表。**@deprecated 2026-08-23**：UI 主题资产管理功能全面移除，管理端 CRUD 接口下线，小程序端改为静态内置；表结构保留以避免影响存量数据，所有读写路由已删除。历史表，未补 is_deleted。

```sql
-- 47. mini_app_config (Schema v15 · @deprecated 2026-08-23 UI 主题资产管理功能全面移除)
-- v59 变更：管理端 CRUD 接口下线，小程序端改为静态内置；表结构保留以避免影响存量数据，
--          所有读写路由（routes/miniAppConfig.js、routes/admin/miniAppConfigRoutes.js）已删除。
--          详见 2026-08-23-需求-商户管理端导航重构与权限矩阵调整.md §五
CREATE TABLE IF NOT EXISTS `mini_app_config` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `config_key` VARCHAR(64) NOT NULL COMMENT '配置项键，如 dock',
  `status` VARCHAR(16) NOT NULL COMMENT 'draft|published|archived',
  `version` INT NOT NULL DEFAULT 0 COMMENT 'draft=0；published/archived 递增版本号',
  `config_json` JSON NOT NULL,
  `published_at` DATETIME DEFAULT NULL,
  `created_by` BIGINT DEFAULT NULL COMMENT 'admin_id',
  `created_at` DATETIME NOT NULL,
  `updated_at` DATETIME NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_key_status_version` (`config_key`, `status`, `version`),
  KEY `idx_key_status` (`config_key`, `status`),
  KEY `idx_key_version` (`config_key`, `version`),
  CONSTRAINT `fk_mini_app_config_admin` FOREIGN KEY (`created_by`) REFERENCES `admin_user` (`admin_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `admin_role`

管理员角色表（Schema v37 · BE-P1-03 · v59 角色增删）。历史字典表，未补 is_deleted。

```sql
-- 72. admin_role (Schema v37 · 管理员角色表 BE-P1-03 · v59 角色增删)
-- v59 变更（2026-08-23）：
--   - 新增 customer_service（客服专员）
--   - 禁用 purchaser / finance（is_active=0，保留 role_id 避免外键断裂）
--   - 启用角色全集：super_admin / warehouse_admin / operations / customer_service
--   - 禁用角色全集：purchaser / finance
CREATE TABLE IF NOT EXISTS `admin_role` (
  `role_id` BIGINT NOT NULL AUTO_INCREMENT,
  `role_name` VARCHAR(64) NOT NULL COMMENT '角色名称',
  `role_code` VARCHAR(32) NOT NULL COMMENT '角色编码（super_admin/warehouse_admin/operations/customer_service 启用；purchaser/finance 已禁用）',
  `description` VARCHAR(255) DEFAULT NULL,
  `is_active` BOOLEAN NOT NULL DEFAULT TRUE,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`role_id`),
  UNIQUE KEY `uk_role_code` (`role_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='管理员角色表';
```

#### 表名 `admin_role_permission`

角色权限关联表（Schema v37 · BE-P1-03）。纯关联表，未补 is_deleted/updated_at。

```sql
-- 73. admin_role_permission (Schema v37 · 角色权限关联表 BE-P1-03)
CREATE TABLE IF NOT EXISTS `admin_role_permission` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `role_id` BIGINT NOT NULL,
  `permission_code` VARCHAR(64) NOT NULL COMMENT '权限标识（40 个权限码全集，详见 utils/adminPermissions.js）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_role_permission` (`role_id`, `permission_code`),
  CONSTRAINT `fk_role_permission_role` FOREIGN KEY (`role_id`) REFERENCES `admin_role` (`role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='角色权限关联表';
```

#### 表名 `admin_permission`

管理员权限码字典表（Schema v44 · v59 权限矩阵调整）。历史字典表，未补 is_deleted。

```sql
-- 73.1 admin_permission (Schema v44 · 管理员权限码字典表 · v59 权限矩阵调整)
-- v59 变更（2026-08-23）：
--   - 清理 18 个废弃权限码（message:* 2 个、mini_app_config:* 3 个、inventory 孤儿 12 个、role:manage 1 个）
--   - 保留权限码全集 40 个，按模块分组：dashboard(1) / order(4) / product(4) / inventory(5) /
--     user(8) / pet(2) / content(6) / registration(3) / workbench(2) / task_platform(4) / system(2)
--   - 详细权限码定义见 utils/adminPermissions.js · PERMISSION_MODULES
CREATE TABLE IF NOT EXISTS `admin_permission` (
  `permission_code` VARCHAR(64) NOT NULL COMMENT '权限码（如 order:ship, product:edit）',
  `module` VARCHAR(32) NOT NULL COMMENT '所属模块（dashboard/order/product/inventory/user/pet/content/registration/workbench/task_platform/system）',
  `name` VARCHAR(64) NOT NULL COMMENT '权限中文名（如 订单发货）',
  `description` VARCHAR(255) DEFAULT NULL COMMENT '权限描述',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序权重（同模块内升序）',
  `is_active` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '是否启用：1-启用 0-禁用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间（北京时间）',
  PRIMARY KEY (`permission_code`),
  KEY `idx_module` (`module`),
  KEY `idx_sort_order` (`sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='管理员权限码字典表';
```

#### 表名 `admin_message_template`

管理员消息模板表（v46 基础字段合规）。

```sql
-- 95. admin_message_template · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `admin_message_template` (
  `template_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `template_code` VARCHAR(64) NOT NULL COMMENT '模板编码（唯一）',
  `template_name` VARCHAR(128) NOT NULL COMMENT '模板名称',
  `category` VARCHAR(32) NOT NULL COMMENT '分类：system/order/pet/marketing/notice/promotion',
  `title_template` VARCHAR(128) NOT NULL COMMENT '标题模板（支持占位符 {var}）',
  `content_template` VARCHAR(1000) NOT NULL COMMENT '内容模板（支持占位符 {var}）',
  `content_type` VARCHAR(16) NOT NULL DEFAULT 'text' COMMENT '内容类型：text/markdown/html',
  `variables_json` JSON DEFAULT NULL COMMENT '可用变量说明 JSON：[{name, desc, example}]',
  `message_type` VARCHAR(32) DEFAULT NULL COMMENT '消息类型编码：对应 user_message.message_type',
  `priority` TINYINT NOT NULL DEFAULT 0 COMMENT '优先级：0-普通 1-重要 2-紧急',
  `default_action_path` VARCHAR(255) DEFAULT NULL COMMENT '默认跳转路径',
  `target_type` VARCHAR(16) NOT NULL DEFAULT 'all_users' COMMENT '默认目标类型：all_users/specific_users/user_group',
  `channel_push` TINYINT NOT NULL DEFAULT 1 COMMENT '小程序推送通道：1-启用 0-禁用',
  `channel_sms` TINYINT NOT NULL DEFAULT 0 COMMENT '短信通道：1-启用 0-禁用',
  `channel_email` TINYINT NOT NULL DEFAULT 0 COMMENT '邮件通道：1-启用 0-禁用',
  `usage_count` INT NOT NULL DEFAULT 0 COMMENT '累计使用次数',
  `last_used_at` DATETIME DEFAULT NULL COMMENT '上次使用时间',
  `created_by` BIGINT DEFAULT NULL COMMENT '创建人 admin_id',
  `updated_by` BIGINT DEFAULT NULL COMMENT '最后更新人 admin_id',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序值',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-停用 1-启用',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`template_id`),
  UNIQUE KEY `uk_template_code` (`template_code`),
  KEY `idx_category_status` (`category`, `status`),
  KEY `idx_status_sort` (`status`, `sort_order`),
  CONSTRAINT `fk_admin_message_template_created_by` FOREIGN KEY (`created_by`) REFERENCES `admin_user` (`admin_id`) ON DELETE SET NULL,
  CONSTRAINT `fk_admin_message_template_updated_by` FOREIGN KEY (`updated_by`) REFERENCES `admin_user` (`admin_id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='管理员消息模板表';
```

### 模块 10：客服会话

#### 表名 `service_conversation`

客服会话表（Schema v16）。

```sql
-- 48. service_conversation (Schema v16)
CREATE TABLE IF NOT EXISTS `service_conversation` (
  `conversation_id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `merchant_id` INT NOT NULL DEFAULT 1 COMMENT '一期固定单商户',
  `active_order_id` BIGINT DEFAULT NULL COMMENT '当前咨询订单',
  `active_product_id` BIGINT DEFAULT NULL COMMENT '商品咨询上下文',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1=open 2=closed',
  `last_message_at` DATETIME DEFAULT NULL,
  `last_message_preview` VARCHAR(200) DEFAULT NULL,
  `unread_admin` INT NOT NULL DEFAULT 0,
  `unread_user` INT NOT NULL DEFAULT 0,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`conversation_id`),
  UNIQUE KEY `uk_user_merchant` (`user_id`, `merchant_id`),
  KEY `idx_merchant_last_message` (`merchant_id`, `last_message_at`),
  KEY `idx_active_order` (`active_order_id`),
  CONSTRAINT `fk_service_conv_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`),
  CONSTRAINT `fk_service_conv_order` FOREIGN KEY (`active_order_id`) REFERENCES `order_main` (`order_id`),
  CONSTRAINT `fk_service_conv_product` FOREIGN KEY (`active_product_id`) REFERENCES `product` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 表名 `service_message`

客服会话消息表，会话消息类，通过业务状态/归档管理，豁免 is_deleted。

```sql
-- 49. service_message (Schema v16)
CREATE TABLE IF NOT EXISTS `service_message` (
  `message_id` BIGINT NOT NULL AUTO_INCREMENT,
  `conversation_id` BIGINT NOT NULL,
  `sender_role` ENUM('user','admin','system') NOT NULL,
  `sender_admin_id` BIGINT DEFAULT NULL COMMENT '管理员发送者 admin_id（仅 sender_role=admin 时填入）',
  `content_type` ENUM('text','image') NOT NULL,
  `content` TEXT NOT NULL,
  `related_order_id` BIGINT DEFAULT NULL COMMENT '发送时快照的关联订单',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  PRIMARY KEY (`message_id`),
  KEY `idx_conv_time` (`conversation_id`, `created_at`),
  KEY `idx_related_order` (`related_order_id`),
  KEY `idx_sender_admin_id` (`sender_admin_id`),
  CONSTRAINT `fk_service_msg_conv` FOREIGN KEY (`conversation_id`) REFERENCES `service_conversation` (`conversation_id`),
  CONSTRAINT `fk_service_msg_order` FOREIGN KEY (`related_order_id`) REFERENCES `order_main` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='客服会话消息表（会话消息表，豁免 is_deleted，通过业务状态/归档管理）';
```

### 模块 11：进销存

#### 表名 `warehouse`

仓库表（Schema v32 · 进销存一期）。

```sql
-- 56. warehouse (Schema v32 · 进销存一期-仓库表)
CREATE TABLE IF NOT EXISTS `warehouse` (
  `warehouse_id` BIGINT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(64) NOT NULL,
  `address` VARCHAR(255) DEFAULT NULL,
  `manager` VARCHAR(32) DEFAULT NULL,
  `permission_config` JSON DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`warehouse_id`),
  KEY `idx_warehouse_is_deleted` (`is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='仓库表';
```

#### 表名 `location`

库位表（Schema v32 · 进销存一期）。

```sql
-- 57. location (Schema v32 · 进销存一期-库位表)
CREATE TABLE IF NOT EXISTS `location` (
  `location_id` BIGINT NOT NULL AUTO_INCREMENT,
  `location_code` VARCHAR(32) NOT NULL,
  `warehouse_id` BIGINT NOT NULL,
  `location_type` VARCHAR(32) NOT NULL DEFAULT 'NORMAL',
  `status` TINYINT NOT NULL DEFAULT 1,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`location_id`),
  UNIQUE KEY `uk_location_code` (`location_code`),
  KEY `idx_location_warehouse_id` (`warehouse_id`),
  KEY `idx_location_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_location_warehouse` FOREIGN KEY (`warehouse_id`) REFERENCES `warehouse` (`warehouse_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='库位表';
```

#### 表名 `inventory_real_time`

库存实时表（Schema v32 · 进销存一期，联合主键 sku_id+warehouse_id+location_id）。

```sql
-- 58. inventory_real_time (Schema v32 · 进销存一期-库存实时表)
CREATE TABLE IF NOT EXISTS `inventory_real_time` (
  `sku_id` BIGINT NOT NULL,
  `warehouse_id` BIGINT NOT NULL,
  `location_id` BIGINT NOT NULL DEFAULT 1,
  `available_qty` INT NOT NULL DEFAULT 0,
  `locked_qty` INT NOT NULL DEFAULT 0,
  `in_transit_qty` INT NOT NULL DEFAULT 0,
  `safety_stock_threshold` INT DEFAULT 0,
  `version` INT NOT NULL DEFAULT 1,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`sku_id`, `warehouse_id`, `location_id`),
  KEY `idx_inventory_real_time_sku_id` (`sku_id`),
  KEY `idx_inventory_real_time_warehouse_id` (`warehouse_id`),
  KEY `idx_inventory_real_time_location_id` (`location_id`),
  KEY `idx_inventory_real_time_warehouse_available` (`warehouse_id`, `available_qty`),
  CONSTRAINT `fk_inventory_real_time_sku` FOREIGN KEY (`sku_id`) REFERENCES `product_sku` (`sku_id`),
  CONSTRAINT `fk_inventory_real_time_warehouse` FOREIGN KEY (`warehouse_id`) REFERENCES `warehouse` (`warehouse_id`),
  CONSTRAINT `fk_inventory_real_time_location` FOREIGN KEY (`location_id`) REFERENCES `location` (`location_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='库存实时表';
```

#### 表名 `inventory_flow`

库存流水表，仅追加不删除，豁免 is_deleted。

```sql
-- 59. inventory_flow (Schema v32 · 进销存一期-库存流水表)
CREATE TABLE IF NOT EXISTS `inventory_flow` (
  `flow_id` VARCHAR(36) NOT NULL COMMENT '主键（UUID）',
  `related_doc_no` VARCHAR(64) DEFAULT NULL,
  `sku_id` BIGINT NOT NULL,
  `warehouse_id` BIGINT NOT NULL,
  `location_id` BIGINT DEFAULT 1,
  `batch_id` BIGINT DEFAULT NULL,
  `direction` VARCHAR(8) NOT NULL,
  `quantity` INT NOT NULL,
  `unit_cost` DECIMAL(10,2) DEFAULT NULL,
  `operator` VARCHAR(32) DEFAULT NULL,
  `reason` VARCHAR(255) DEFAULT NULL,
  `timestamp` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '业务发生时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  PRIMARY KEY (`flow_id`),
  KEY `idx_inventory_flow_sku_id` (`sku_id`),
  KEY `idx_inventory_flow_batch_id` (`batch_id`),
  KEY `idx_inventory_flow_timestamp` (`timestamp`),
  KEY `idx_inventory_flow_related_doc_no` (`related_doc_no`),
  KEY `idx_inventory_flow_direction` (`direction`),
  CONSTRAINT `fk_inventory_flow_sku` FOREIGN KEY (`sku_id`) REFERENCES `product_sku` (`sku_id`),
  CONSTRAINT `fk_inventory_flow_warehouse` FOREIGN KEY (`warehouse_id`) REFERENCES `warehouse` (`warehouse_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='库存流水表（仅追加不删除，豁免 is_deleted）';
```

#### 表名 `sales_outbound`

销售出库单表（Schema v32 · v34 扩展 total_cost）。

```sql
-- 60. sales_outbound (Schema v32 · 进销存一期-销售出库单表 · v34 扩展 total_cost)
CREATE TABLE IF NOT EXISTS `sales_outbound` (
  `outbound_id` BIGINT NOT NULL AUTO_INCREMENT,
  `order_id` BIGINT DEFAULT NULL,
  `outbound_details` JSON DEFAULT NULL,
  `total_cost` DECIMAL(12,2) DEFAULT 0 COMMENT '出库总成本（Schema v34 扩展）',
  `batch_id` BIGINT DEFAULT NULL,
  `status` VARCHAR(32) NOT NULL DEFAULT 'pending',
  `logistics_info` JSON DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`outbound_id`),
  UNIQUE KEY `uk_sales_outbound_order_id` (`order_id`),
  KEY `idx_sales_outbound_status` (`status`),
  KEY `idx_sales_outbound_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_sales_outbound_order` FOREIGN KEY (`order_id`) REFERENCES `order_main` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='销售出库单表';
```

#### 表名 `inventory_adjust`

库存调整单表（Schema v32 · v33 扩展 batch_id/source_type/source_id）。

```sql
-- 61. inventory_adjust (Schema v32 · 进销存一期-库存调整单表 · v33 扩展 batch_id/source_type/source_id)
CREATE TABLE IF NOT EXISTS `inventory_adjust` (
  `adjust_id` BIGINT NOT NULL AUTO_INCREMENT,
  `adjust_no` VARCHAR(32) NOT NULL,
  `warehouse_id` BIGINT NOT NULL,
  `sku_id` BIGINT NOT NULL,
  `location_id` BIGINT DEFAULT 1,
  `batch_id` BIGINT DEFAULT NULL COMMENT '批次ID（Schema v33 扩展）',
  `before_qty` INT NOT NULL,
  `after_qty` INT NOT NULL,
  `reason` VARCHAR(255) DEFAULT NULL,
  `source_type` VARCHAR(32) DEFAULT NULL COMMENT '来源单据类型（Schema v33 扩展）',
  `source_id` BIGINT DEFAULT NULL COMMENT '来源单据ID（Schema v33 扩展）',
  `status` VARCHAR(32) NOT NULL DEFAULT 'pending',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`adjust_id`),
  UNIQUE KEY `uk_adjust_no` (`adjust_no`),
  KEY `idx_inventory_adjust_warehouse_id` (`warehouse_id`),
  KEY `idx_inventory_adjust_sku_id` (`sku_id`),
  KEY `idx_inventory_adjust_status` (`status`),
  KEY `idx_inventory_adjust_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_inventory_adjust_warehouse` FOREIGN KEY (`warehouse_id`) REFERENCES `warehouse` (`warehouse_id`),
  CONSTRAINT `fk_inventory_adjust_sku` FOREIGN KEY (`sku_id`) REFERENCES `product_sku` (`sku_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='库存调整单表';
```

#### 表名 `batch_info`

批次信息表（Schema v33 · v34 扩展 unit_cost）。

```sql
-- 62. batch_info (Schema v33 · 进销存二期-批次信息表 · v34 扩展 unit_cost)
CREATE TABLE IF NOT EXISTS `batch_info` (
  `batch_id` BIGINT NOT NULL AUTO_INCREMENT,
  `batch_no` VARCHAR(64) NOT NULL,
  `sku_id` BIGINT NOT NULL,
  `production_date` DATE DEFAULT NULL,
  `expiry_date` DATE DEFAULT NULL,
  `quantity` INT NOT NULL DEFAULT 0,
  `unit_cost` DECIMAL(10,2) DEFAULT 0 COMMENT '单位成本（Schema v34 扩展）',
  `warehouse_id` BIGINT NOT NULL,
  `location_id` BIGINT DEFAULT 1,
  `inspection_result` VARCHAR(32) NOT NULL DEFAULT 'pending',
  `supplier_id` BIGINT DEFAULT NULL,
  `purchase_id` BIGINT DEFAULT NULL,
  `status` VARCHAR(32) NOT NULL DEFAULT 'active',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`batch_id`),
  UNIQUE KEY `uk_batch_no` (`batch_no`),
  KEY `idx_batch_info_sku_id` (`sku_id`),
  KEY `idx_batch_info_warehouse_id` (`warehouse_id`),
  KEY `idx_batch_info_location_id` (`location_id`),
  KEY `idx_batch_info_expiry_date` (`expiry_date`),
  KEY `idx_batch_info_status` (`status`),
  KEY `idx_batch_info_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_batch_info_sku` FOREIGN KEY (`sku_id`) REFERENCES `product_sku` (`sku_id`),
  CONSTRAINT `fk_batch_info_warehouse` FOREIGN KEY (`warehouse_id`) REFERENCES `warehouse` (`warehouse_id`),
  CONSTRAINT `fk_batch_info_location` FOREIGN KEY (`location_id`) REFERENCES `location` (`location_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='批次信息表';
```

#### 表名 `expiry_warning`

临期预警表（Schema v33 · 进销存二期）。

```sql
-- 63. expiry_warning (Schema v33 · 进销存二期-临期预警表)
CREATE TABLE IF NOT EXISTS `expiry_warning` (
  `warning_id` BIGINT NOT NULL AUTO_INCREMENT,
  `batch_id` BIGINT NOT NULL,
  `sku_id` BIGINT NOT NULL,
  `warning_level` VARCHAR(32) NOT NULL,
  `trigger_date` DATETIME NOT NULL,
  `status` VARCHAR(32) NOT NULL DEFAULT 'pending',
  `notify_users` JSON DEFAULT NULL,
  `processed_at` DATETIME DEFAULT NULL,
  `processed_by` BIGINT DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`warning_id`),
  KEY `idx_expiry_warning_batch_id` (`batch_id`),
  KEY `idx_expiry_warning_sku_id` (`sku_id`),
  KEY `idx_expiry_warning_warning_level` (`warning_level`),
  KEY `idx_expiry_warning_status` (`status`),
  KEY `idx_expiry_warning_trigger_date` (`trigger_date`),
  KEY `idx_expiry_warning_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_expiry_warning_batch` FOREIGN KEY (`batch_id`) REFERENCES `batch_info` (`batch_id`),
  CONSTRAINT `fk_expiry_warning_sku` FOREIGN KEY (`sku_id`) REFERENCES `product_sku` (`sku_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='临期预警表';
```

#### 表名 `expiry_warning_config`

临期预警配置表（Schema v36 · BE-P0-04）。历史配置表，未补 is_deleted。

```sql
-- 71. expiry_warning_config (Schema v36 · 临期预警配置表 BE-P0-04)
CREATE TABLE IF NOT EXISTS `expiry_warning_config` (
  `config_id` BIGINT NOT NULL AUTO_INCREMENT,
  `high_threshold_days` INT NOT NULL DEFAULT 7 COMMENT '高等级预警阈值（剩余天数 ≤ 此值触发）',
  `medium_threshold_days` INT NOT NULL DEFAULT 15 COMMENT '中等级预警阈值',
  `low_threshold_days` INT NOT NULL DEFAULT 30 COMMENT '低等级预警阈值',
  `email_notify_enabled` BOOLEAN NOT NULL DEFAULT FALSE COMMENT '启用邮件通知',
  `sms_notify_enabled` BOOLEAN NOT NULL DEFAULT FALSE COMMENT '启用短信通知',
  `system_notify_enabled` BOOLEAN NOT NULL DEFAULT TRUE COMMENT '启用系统消息通知',
  `notify_time` VARCHAR(8) NOT NULL DEFAULT '09:00' COMMENT '每日预警通知发送时间 HH:mm',
  `is_active` BOOLEAN NOT NULL DEFAULT TRUE COMMENT '是否当前生效配置',
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`config_id`),
  KEY `idx_expiry_warning_config_active` (`is_active`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='临期预警配置表';
```

#### 表名 `supplier`

供应商表（Schema v34 · 进销存三期）。

```sql
-- 64. supplier (Schema v34 · 进销存三期-供应商表)
CREATE TABLE IF NOT EXISTS `supplier` (
  `supplier_id` BIGINT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(128) NOT NULL,
  `contact` VARCHAR(64) DEFAULT NULL,
  `phone` VARCHAR(32) DEFAULT NULL,
  `email` VARCHAR(128) DEFAULT NULL,
  `address` VARCHAR(255) DEFAULT NULL,
  `tax_no` VARCHAR(64) DEFAULT NULL,
  `bank_info` JSON DEFAULT NULL,
  `status` VARCHAR(32) NOT NULL DEFAULT 'active',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`supplier_id`),
  KEY `idx_supplier_name` (`name`),
  KEY `idx_supplier_phone` (`phone`),
  KEY `idx_supplier_status` (`status`),
  KEY `idx_supplier_is_deleted` (`is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='供应商表';
```

#### 表名 `purchase_order`

采购单表（Schema v34 · 进销存三期）。

```sql
-- 65. purchase_order (Schema v34 · 进销存三期-采购单表)
CREATE TABLE IF NOT EXISTS `purchase_order` (
  `purchase_id` BIGINT NOT NULL AUTO_INCREMENT,
  `purchase_no` VARCHAR(64) NOT NULL,
  `supplier_id` BIGINT NOT NULL,
  `purchase_details` JSON DEFAULT NULL,
  `status` VARCHAR(32) NOT NULL DEFAULT 'pending',
  `total_amount` DECIMAL(12,2) NOT NULL DEFAULT 0,
  `paid_amount` DECIMAL(12,2) DEFAULT 0,
  `delivery_date` DATETIME DEFAULT NULL,
  `expected_delivery_date` DATETIME DEFAULT NULL,
  `warehouse_id` BIGINT DEFAULT 1,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`purchase_id`),
  UNIQUE KEY `uk_purchase_no` (`purchase_no`),
  KEY `idx_purchase_order_supplier_id` (`supplier_id`),
  KEY `idx_purchase_order_status` (`status`),
  KEY `idx_purchase_order_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_purchase_order_supplier` FOREIGN KEY (`supplier_id`) REFERENCES `supplier` (`supplier_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='采购单表';
```

#### 表名 `purchase_item`

采购明细表（Schema v34 · 进销存三期）。

```sql
-- 66. purchase_item (Schema v34 · 进销存三期-采购明细表)
CREATE TABLE IF NOT EXISTS `purchase_item` (
  `item_id` BIGINT NOT NULL AUTO_INCREMENT,
  `purchase_id` BIGINT NOT NULL,
  `sku_id` BIGINT NOT NULL,
  `quantity` INT NOT NULL DEFAULT 0,
  `received_quantity` INT DEFAULT 0,
  `unit_price` DECIMAL(10,2) NOT NULL DEFAULT 0,
  `total_price` DECIMAL(12,2) DEFAULT 0,
  `batch_id` BIGINT DEFAULT NULL,
  `status` VARCHAR(32) NOT NULL DEFAULT 'pending',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`item_id`),
  KEY `idx_purchase_item_purchase_id` (`purchase_id`),
  KEY `idx_purchase_item_sku_id` (`sku_id`),
  KEY `idx_purchase_item_batch_id` (`batch_id`),
  KEY `idx_purchase_item_status` (`status`),
  KEY `idx_purchase_item_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_purchase_item_purchase` FOREIGN KEY (`purchase_id`) REFERENCES `purchase_order` (`purchase_id`),
  CONSTRAINT `fk_purchase_item_sku` FOREIGN KEY (`sku_id`) REFERENCES `product_sku` (`sku_id`),
  CONSTRAINT `fk_purchase_item_batch` FOREIGN KEY (`batch_id`) REFERENCES `batch_info` (`batch_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='采购明细表';
```

#### 表名 `accounts_payable`

应付账款表（Schema v34 · 进销存三期）。

```sql
-- 67. accounts_payable (Schema v34 · 进销存三期-应付账款表)
CREATE TABLE IF NOT EXISTS `accounts_payable` (
  `ap_id` BIGINT NOT NULL AUTO_INCREMENT,
  `purchase_id` BIGINT NOT NULL,
  `supplier_id` BIGINT NOT NULL,
  `amount` DECIMAL(12,2) NOT NULL DEFAULT 0,
  `paid_amount` DECIMAL(12,2) DEFAULT 0,
  `status` VARCHAR(32) NOT NULL DEFAULT 'pending',
  `due_date` DATETIME DEFAULT NULL,
  `paid_at` DATETIME DEFAULT NULL,
  `payment_method` VARCHAR(32) DEFAULT NULL,
  `payment_no` VARCHAR(64) DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`ap_id`),
  KEY `idx_accounts_payable_purchase_id` (`purchase_id`),
  KEY `idx_accounts_payable_supplier_id` (`supplier_id`),
  KEY `idx_accounts_payable_status` (`status`),
  KEY `idx_accounts_payable_due_date` (`due_date`),
  KEY `idx_accounts_payable_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_accounts_payable_purchase` FOREIGN KEY (`purchase_id`) REFERENCES `purchase_order` (`purchase_id`),
  CONSTRAINT `fk_accounts_payable_supplier` FOREIGN KEY (`supplier_id`) REFERENCES `supplier` (`supplier_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='应付账款表';
```

#### 表名 `accounts_receivable`

应收账款表（Schema v34 · 进销存三期）。

```sql
-- 68. accounts_receivable (Schema v34 · 进销存三期-应收账款表)
CREATE TABLE IF NOT EXISTS `accounts_receivable` (
  `ar_id` BIGINT NOT NULL AUTO_INCREMENT,
  `order_id` BIGINT NOT NULL,
  `user_id` BIGINT NOT NULL,
  `amount` DECIMAL(12,2) NOT NULL DEFAULT 0,
  `paid_amount` DECIMAL(12,2) DEFAULT 0,
  `status` VARCHAR(32) NOT NULL DEFAULT 'pending',
  `due_date` DATETIME DEFAULT NULL,
  `paid_at` DATETIME DEFAULT NULL,
  `payment_method` VARCHAR(32) DEFAULT NULL,
  `payment_no` VARCHAR(64) DEFAULT NULL,
  `cost_amount` DECIMAL(12,2) DEFAULT 0,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `created_by` BIGINT DEFAULT NULL,
  `updated_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`ar_id`),
  KEY `idx_accounts_receivable_order_id` (`order_id`),
  KEY `idx_accounts_receivable_user_id` (`user_id`),
  KEY `idx_accounts_receivable_status` (`status`),
  KEY `idx_accounts_receivable_due_date` (`due_date`),
  KEY `idx_accounts_receivable_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_accounts_receivable_order` FOREIGN KEY (`order_id`) REFERENCES `order_main` (`order_id`),
  CONSTRAINT `fk_accounts_receivable_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='应收账款表';
```

#### 表名 `inventory_transfer`

仓间调拨单表（Schema v33）。

```sql
-- 69. inventory_transfer (Schema v33 · 仓间调拨单表)
CREATE TABLE IF NOT EXISTS `inventory_transfer` (
  `transfer_id` BIGINT NOT NULL AUTO_INCREMENT,
  `transfer_no` VARCHAR(64) NOT NULL,
  `from_warehouse_id` BIGINT NOT NULL,
  `to_warehouse_id` BIGINT NOT NULL,
  `from_location_id` BIGINT DEFAULT NULL,
  `to_location_id` BIGINT DEFAULT NULL,
  `sku_id` BIGINT NOT NULL,
  `batch_id` BIGINT DEFAULT NULL,
  `quantity` INT NOT NULL,
  `reason` VARCHAR(255) DEFAULT NULL,
  `status` VARCHAR(32) NOT NULL DEFAULT 'pending',
  `created_by` BIGINT DEFAULT NULL,
  `approved_by` BIGINT DEFAULT NULL,
  `approved_at` DATETIME DEFAULT NULL,
  `completed_at` DATETIME DEFAULT NULL,
  `cancelled_at` DATETIME DEFAULT NULL,
  `cancelled_by` BIGINT DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`transfer_id`),
  UNIQUE KEY `uk_transfer_no` (`transfer_no`),
  KEY `idx_inventory_transfer_from_warehouse_id` (`from_warehouse_id`),
  KEY `idx_inventory_transfer_to_warehouse_id` (`to_warehouse_id`),
  KEY `idx_inventory_transfer_sku_id` (`sku_id`),
  KEY `idx_inventory_transfer_batch_id` (`batch_id`),
  KEY `idx_inventory_transfer_status` (`status`),
  KEY `idx_inventory_transfer_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_inventory_transfer_from_warehouse` FOREIGN KEY (`from_warehouse_id`) REFERENCES `warehouse` (`warehouse_id`),
  CONSTRAINT `fk_inventory_transfer_to_warehouse` FOREIGN KEY (`to_warehouse_id`) REFERENCES `warehouse` (`warehouse_id`),
  CONSTRAINT `fk_inventory_transfer_from_location` FOREIGN KEY (`from_location_id`) REFERENCES `location` (`location_id`),
  CONSTRAINT `fk_inventory_transfer_to_location` FOREIGN KEY (`to_location_id`) REFERENCES `location` (`location_id`),
  CONSTRAINT `fk_inventory_transfer_sku` FOREIGN KEY (`sku_id`) REFERENCES `product_sku` (`sku_id`),
  CONSTRAINT `fk_inventory_transfer_batch` FOREIGN KEY (`batch_id`) REFERENCES `batch_info` (`batch_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='仓间调拨单表';
```

#### 表名 `inventory_check`

盘点单表（Schema v33）。

```sql
-- 70. inventory_check (Schema v33 · 盘点单表)
CREATE TABLE IF NOT EXISTS `inventory_check` (
  `check_id` BIGINT NOT NULL AUTO_INCREMENT,
  `check_no` VARCHAR(64) NOT NULL,
  `warehouse_id` BIGINT NOT NULL,
  `location_id` BIGINT DEFAULT NULL,
  `sku_id` BIGINT DEFAULT NULL,
  `batch_id` BIGINT DEFAULT NULL,
  `system_qty` INT DEFAULT NULL,
  `actual_qty` INT DEFAULT NULL,
  `diff_qty` INT DEFAULT NULL,
  `reason` VARCHAR(255) DEFAULT NULL,
  `status` VARCHAR(32) NOT NULL DEFAULT 'pending',
  `created_by` BIGINT DEFAULT NULL,
  `checked_by` BIGINT DEFAULT NULL,
  `checked_at` DATETIME DEFAULT NULL,
  `adjusted_at` DATETIME DEFAULT NULL,
  `adjusted_by` BIGINT DEFAULT NULL,
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`check_id`),
  UNIQUE KEY `uk_check_no` (`check_no`),
  KEY `idx_inventory_check_warehouse_id` (`warehouse_id`),
  KEY `idx_inventory_check_location_id` (`location_id`),
  KEY `idx_inventory_check_sku_id` (`sku_id`),
  KEY `idx_inventory_check_batch_id` (`batch_id`),
  KEY `idx_inventory_check_status` (`status`),
  KEY `idx_inventory_check_is_deleted` (`is_deleted`),
  CONSTRAINT `fk_inventory_check_warehouse` FOREIGN KEY (`warehouse_id`) REFERENCES `warehouse` (`warehouse_id`),
  CONSTRAINT `fk_inventory_check_location` FOREIGN KEY (`location_id`) REFERENCES `location` (`location_id`),
  CONSTRAINT `fk_inventory_check_sku` FOREIGN KEY (`sku_id`) REFERENCES `product_sku` (`sku_id`),
  CONSTRAINT `fk_inventory_check_batch` FOREIGN KEY (`batch_id`) REFERENCES `batch_info` (`batch_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='盘点单表';
```

### 模块 12：通知与广播

#### 表名 `notification`

通知消息表（Schema v38 · BE-P3-01）。历史表，未补 is_deleted。

```sql
-- 74. notification (Schema v38 · 通知消息表 BE-P3-01)
CREATE TABLE IF NOT EXISTS `notification` (
  `notification_id` BIGINT NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(128) NOT NULL COMMENT '通知标题',
  `content` TEXT NOT NULL COMMENT '通知内容',
  `notification_type` VARCHAR(32) NOT NULL COMMENT '通知类型：expiry_warning / low_stock / manual / system',
  `priority` TINYINT NOT NULL DEFAULT 0 COMMENT '优先级：0=普通 1=重要 2=紧急',
  `status` VARCHAR(16) NOT NULL DEFAULT 'unread' COMMENT '状态：unread / read / archived',
  `target_type` VARCHAR(16) NOT NULL DEFAULT 'all_admin' COMMENT '目标类型：all_admin / specific_admin',
  `target_admin_id` BIGINT DEFAULT NULL COMMENT '目标管理员ID（target_type=specific_admin 时有效）',
  `biz_type` VARCHAR(32) DEFAULT NULL COMMENT '业务类型：expiry_warning / low_stock / batch / sku',
  `biz_id` BIGINT DEFAULT NULL COMMENT '业务ID（如 warning_id / sku_id）',
  `extra_json` TEXT DEFAULT NULL COMMENT '扩展信息JSON',
  `created_by` BIGINT DEFAULT NULL COMMENT '创建人 admin_id',
  `read_at` DATETIME DEFAULT NULL COMMENT '阅读时间',
  `idempotency_key` VARCHAR(128) DEFAULT NULL COMMENT '幂等键，防止 cron 重复推送',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`notification_id`),
  INDEX `idx_notification_type_status` (`notification_type`, `status`),
  INDEX `idx_notification_target` (`target_type`, `target_admin_id`),
  INDEX `idx_notification_created` (`created_at`),
  INDEX `idx_notification_idempotency` (`idempotency_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='通知消息表';
```

#### 表名 `preset_broadcast_message`

预设广播消息模板（Schema v54 · 信息注册台）。

```sql
-- 96. preset_broadcast_message (Schema v54 · 信息注册台 · 预设广播消息模板)
CREATE TABLE IF NOT EXISTS `preset_broadcast_message` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `title` VARCHAR(128) NOT NULL COMMENT '广播标题',
  `content` VARCHAR(500) NOT NULL COMMENT '广播内容',
  `target_type` VARCHAR(32) NOT NULL DEFAULT 'all' COMMENT '目标类型：all-全部用户；users-指定用户',
  `target_value` TEXT NULL COMMENT '指定用户时存逗号分隔 user_id',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-启用；0-禁用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未删除；1-已逻辑删除',
  PRIMARY KEY (`id`),
  KEY `idx_preset_status_sort` (`status`, `sort_order`),
  KEY `idx_preset_is_deleted` (`is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='预设广播消息模板';
```

#### 表名 `reminder_config`

日常提醒配置表（v46 基础字段合规）。

```sql
-- 94. reminder_config · v46 基础字段合规
CREATE TABLE IF NOT EXISTS `reminder_config` (
  `reminder_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `pet_id` BIGINT DEFAULT NULL COMMENT '关联宠物ID（空=用户级提醒）',
  `reminder_type` VARCHAR(32) NOT NULL COMMENT '提醒类型：vaccine/deworm/health_check/meal/exercise/grooming/medication/birthday/custom',
  `title` VARCHAR(128) NOT NULL COMMENT '提醒标题',
  `content` VARCHAR(500) DEFAULT NULL COMMENT '提醒内容',
  `tag_id` INT DEFAULT NULL COMMENT '提醒标签ID（关联 reminder_tag_dict.id）',
  `repeat_type` VARCHAR(16) NOT NULL DEFAULT 'once' COMMENT '重复类型：once/daily/weekly/monthly/yearly/custom_cron',
  `repeat_cron` VARCHAR(64) DEFAULT NULL COMMENT '自定义 cron 表达式',
  `remind_time` VARCHAR(5) NOT NULL DEFAULT '09:00' COMMENT '提醒时刻 HH:mm',
  `next_trigger_time` DATETIME NOT NULL COMMENT '下次触发时间',
  `advance_remind_minutes` SMALLINT NOT NULL DEFAULT 0 COMMENT '提前提醒分钟数',
  `notify_push` TINYINT NOT NULL DEFAULT 1 COMMENT '推送通知：1-开启 0-关闭',
  `notify_email` TINYINT NOT NULL DEFAULT 0 COMMENT '邮件通知：1-开启 0-关闭',
  `notify_sms` TINYINT NOT NULL DEFAULT 0 COMMENT '短信通知：1-开启 0-关闭',
  `attachments` JSON DEFAULT NULL COMMENT '附件URL数组',
  `biz_type` VARCHAR(32) DEFAULT NULL COMMENT '关联业务类型',
  `biz_id` BIGINT DEFAULT NULL COMMENT '关联业务ID',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-停用 1-启用 2-已完成/已触发',
  `trigger_count` INT NOT NULL DEFAULT 0 COMMENT '已触发次数',
  `last_trigger_time` DATETIME DEFAULT NULL COMMENT '上次触发时间',
  `extra_json` TEXT COMMENT '扩展字段JSON',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间（北京时间）',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已逻辑删除',
  PRIMARY KEY (`reminder_id`),
  KEY `idx_user_pet_status` (`user_id`, `pet_id`, `status`),
  KEY `idx_next_trigger_time` (`next_trigger_time`),
  KEY `idx_reminder_type` (`reminder_type`),
  KEY `idx_biz` (`biz_type`, `biz_id`),
  CONSTRAINT `fk_reminder_config_user` FOREIGN KEY (`user_id`) REFERENCES `user_account` (`user_id`),
  CONSTRAINT `fk_reminder_config_pet` FOREIGN KEY (`pet_id`) REFERENCES `pet` (`pet_id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='日常提醒配置表';
```

#### 表名 `broadcast_schedule`

消息广播调度表（Schema v57 · 任务可视化调度平台）。

```sql
-- 103. broadcast_schedule (Schema v57 · 任务可视化调度平台 · 消息广播调度)
CREATE TABLE IF NOT EXISTS `broadcast_schedule` (
  `schedule_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '调度主键',
  `preset_message_id` BIGINT NOT NULL COMMENT '预设广播消息ID（FK preset_broadcast_message.id）',
  `scheduled_at` DATETIME NOT NULL COMMENT '计划发送时间（北京时间）',
  `broadcast_status` TINYINT NOT NULL DEFAULT 0 COMMENT '调度状态：0-待发送；1-发送中；2-已发送；3-已撤销；4-失败',
  `admin_message_job_id` BIGINT NULL COMMENT '已发送时关联的即时广播任务ID（FK admin_message_job.job_id）',
  `created_by_admin_id` BIGINT NULL COMMENT '创建管理员ID（FK admin_user.admin_id，ON DELETE SET NULL；管理员注销时置 NULL）',
  `cancelled_by_admin_id` BIGINT NULL COMMENT '撤销管理员ID',
  `cancelled_at` DATETIME NULL COMMENT '撤销时间（北京时间）',
  `sent_at` DATETIME NULL COMMENT '实际发送时间（北京时间）',
  `error_message` VARCHAR(500) NULL COMMENT '失败时的错误信息',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未删除；1-已逻辑删除',
  PRIMARY KEY (`schedule_id`),
  UNIQUE KEY `uk_preset_scheduled_del` (`preset_message_id`, `scheduled_at`, `is_deleted`),
  KEY `idx_status_scheduled` (`broadcast_status`, `scheduled_at`),
  KEY `idx_created_by` (`created_by_admin_id`),
  CONSTRAINT `fk_broadcast_schedule_preset` FOREIGN KEY (`preset_message_id`) REFERENCES `preset_broadcast_message` (`id`) ON DELETE RESTRICT,
  CONSTRAINT `fk_broadcast_schedule_creator` FOREIGN KEY (`created_by_admin_id`) REFERENCES `admin_user` (`admin_id`) ON DELETE SET NULL,
  CONSTRAINT `fk_broadcast_schedule_canceler` FOREIGN KEY (`cancelled_by_admin_id`) REFERENCES `admin_user` (`admin_id`) ON DELETE SET NULL,
  CONSTRAINT `fk_broadcast_schedule_job` FOREIGN KEY (`admin_message_job_id`) REFERENCES `admin_message_job` (`job_id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='消息广播调度表';
```

### 模块 13：工作台

#### 表名 `workbench_draft`

小程序工作台草稿/版本/发布表。

```sql
-- 100. workbench_draft (小程序工作台 · 草稿/版本/发布表)
CREATE TABLE `workbench_draft` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `module_type` VARCHAR(32) NOT NULL COMMENT '模块类型：product_publish/article_publish/cms_publish/content_config',
  `version_no` INT NOT NULL COMMENT '版本编号（同 module_type 内递增）',
  `draft_data` JSON NOT NULL COMMENT '草稿内容（模块特定结构）',
  `publish_status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-草稿；1-已发布；2-已归档',
  `published_at` DATETIME NULL COMMENT '发布时间（publish_status=1 或 2 时有值）',
  `published_by` BIGINT NULL COMMENT '发布操作员 admin_user_id',
  `remark` VARCHAR(500) NULL COMMENT '版本备注',
  `created_by` BIGINT NOT NULL COMMENT '创建草稿操作员 admin_user_id',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未删除；1-已逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_module_version_del` (`module_type`, `version_no`, `is_deleted`),
  KEY `idx_module_status` (`module_type`, `publish_status`),
  KEY `idx_module_created` (`module_type`, `created_at`),
  KEY `idx_created_by` (`created_by`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='小程序工作台草稿/版本/发布表';
```

#### 表名 `workbench_item_blocklist`

小程序工作台发布项禁止列表。

```sql
-- 101. workbench_item_blocklist (小程序工作台 · 发布项禁止列表)
CREATE TABLE `workbench_item_blocklist` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `module_type` VARCHAR(32) NOT NULL COMMENT '模块类型：product_publish/article_publish',
  `item_type` VARCHAR(32) NOT NULL COMMENT '项目类型：product/product_category/article（资讯 W7 扩展）',
  `item_id` BIGINT NOT NULL COMMENT '关联项目 ID',
  `blocked_reason` VARCHAR(255) NULL COMMENT '禁止原因（如信息注册台软删除联动下架）',
  `blocked_by` BIGINT NULL COMMENT '操作员 admin_user_id',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未删除；1-已逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_item_del` (`module_type`, `item_type`, `item_id`, `is_deleted`),
  KEY `idx_item_type_id` (`item_type`, `item_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='小程序工作台发布项禁止列表';
```

#### 表名 `cron_task_execution_log`

定时任务执行日志表（Schema v57 · 任务可视化调度平台），仅追加流水表，§8.5 豁免 is_deleted。

```sql
-- 102. cron_task_execution_log (Schema v57 · 任务可视化调度平台 · 定时任务执行日志)
CREATE TABLE IF NOT EXISTS `cron_task_execution_log` (
  `log_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '日志主键',
  `task_code` VARCHAR(64) NOT NULL COMMENT '任务编码（如 cron:order-expiry）',
  `task_name` VARCHAR(64) NOT NULL COMMENT '任务名称（冗余便于查询）',
  `trigger_type` TINYINT NOT NULL DEFAULT 0 COMMENT '触发类型：0-定时调度；1-管理员手动触发',
  `triggered_by_admin_id` BIGINT NULL COMMENT '手动触发时记录管理员ID（定时调度为NULL）',
  `start_time` DATETIME NOT NULL COMMENT '执行开始时间（北京时间）',
  `end_time` DATETIME NULL COMMENT '执行结束时间（北京时间）',
  `duration_ms` INT NULL COMMENT '执行耗时（毫秒）',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '执行状态：0-运行中；1-成功；2-失败；3-超时',
  `result_summary` JSON NULL COMMENT '执行结果摘要（如 {scanned:10, processed:5}）',
  `error_message` TEXT NULL COMMENT '失败时的错误信息',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '记录创建时间（北京时间）',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '记录最后修改时间',
  PRIMARY KEY (`log_id`),
  KEY `idx_task_code_start` (`task_code`, `start_time`),
  KEY `idx_trigger_type` (`trigger_type`),
  KEY `idx_triggered_by_admin` (`triggered_by_admin_id`),
  CONSTRAINT `fk_cron_exec_admin` FOREIGN KEY (`triggered_by_admin_id`) REFERENCES `admin_user` (`admin_id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='定时任务执行日志（仅追加流水表，§8.5 豁免 is_deleted）';
```

---

## 四、附录：schema.sql 公共声明

以下为 schema.sql 文件头部与尾部的公共声明，不属于任何单表，但为保证建表语句完整性一并列出。

### 4.1 文件头部声明

```sql
-- pet-app-backend schema
-- MySQL 8.0+ utf8mb4

SET NAMES utf8mb4;
SET FOREIGN_KEY_CHECKS = 0;

CREATE DATABASE IF NOT EXISTS pet_app
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE pet_app;
```

### 4.2 文件尾部声明

```sql
SET FOREIGN_KEY_CHECKS = 1;
```

### 4.3 已物理删除表说明

以下两张表已于 Schema v51 物理删除，本档不再收录建表语句，仅保留 schema.sql 中的注释说明：

```sql
-- 53. pet_meal_session_pet (Schema v27 · 多宠物联合日记录 - 餐食会话-宠物关联)
-- 已于 Schema v51 物理删除（宠物联合记录降级备案，详见 pet-app-common/docs/requirements/2026-08-19-需求-宠物联合记录与最近状态降级备案.md）

-- 54. pet_exercise_session_pet (Schema v27 · 多宠物联合日记录 - 运动会话-宠物关联)
-- 已于 Schema v51 物理删除（宠物联合记录降级备案）
```

